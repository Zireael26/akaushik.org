- 2026-08-23 — Cleaned stale current-code comments that referenced deleted parchment-era systems. `app/page.tsx`: removed stale `components/sections/Contact.tsx still exists` claim — contact now lives in the footer (`components/site/SiteFooter.tsx` / `#contact` marquee+CTA) and the file was deleted in the pixel dead-code sweep (spec 004 T30). `app/layout.tsx`, `app/styles/tokens.css`, `lib/pixel-theme.ts`, `lib/scenes/theme-switch.ts`: replaced `components/site/ThemeToggle.tsx` with `components/pixel/ThemeSwitch.tsx` (canvas island via `lib/scenes/theme-switch.ts` / `mountThemeSwitch`), the 52×26 pixel ThemeSwitch that superseded the old toggle; updated both the bootstrap comment (`public/init-theme.js` alignment) and the storage-key comment (`Mode: abhishek.portfolio.mode`). `components/sections/Hero.tsx`: retained accurate `TweakBridge that drove it is gone` note — not stale, records why tagline A/B/C collapsed to one line; no `AgentGraph`/`WandererCrane` runtime comments remain in `app/**`, `components/**`, `lib/**`, `scripts/**`, or active CSS. No behavior, exports, or generated files changed; `e2e/**`, `docs/CHANGELOG.md`, ADRs, `docs/BUNDLE_BUDGET.md` snapshots, `proxy.ts`, `lib/agent-proxy.ts`, `worker/index.ts`, `package.json`, and `pnpm-lock.yaml` untouched. `next-mdx-remote` runtime audit: zero runtime imports in shipped code — `lib/mdx-options.ts` is `import type { MDXRemote }` (type-only), `lib/mdx-options.test.ts` imports `serialize` from `next-mdx-remote/serialize` (test-only proxy for the shiki v4 dual-theme contract), `scripts/build-mdx-modules.ts` mentions the old per-request compiler only in a historical comment and now compiles via `@mdx-js/mdx`. Production rendering uses pre-compiled modules from `lib/mdx/generated` via `@mdx-js/mdx`+`rehype-pretty-code`+`shiki` (`scripts/build-mdx-modules.ts`), verified by absence of any `from 'next-mdx-remote` runtime import in `app/**`, `components/**`, or `lib/**` outside the two noted non-runtime cases. Conclusion: `package.json` **cannot drop `next-mdx-remote` now** without breaking the test suite — removing it would fail `lib/mdx-options.test.ts` (`serialize`) and lose the type for `MDXRemoteOptions`; to drop, either delete/rewrite that test to use `@mdx-js/mdx` directly or move the dependency to `devDependencies` (runtime would be safe, test/not-type would not). Dependencies unchanged per brief.
