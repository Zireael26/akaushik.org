- 2026-08-23 — Added deterministic Vitest policy test `lib/tokens-contrast.test.ts` that reads `app/styles/tokens.css`, parses `:root` and `[data-mode='dark']` blocks, enumerates every `--ink*` and `--px-*-ink` token (no hard-coded names beyond the prefixes), parses the hex and `rgba()` forms present, composites alpha colors over that theme's `--bg`, computes WCAG relative luminance and contrast, and asserts ≥4.5:1 in both themes, failing with `theme token ratio` detail so future additions are covered and silent contrast regressions are caught.
