- 2026-08-23 — Finished Playwright selector migration from baseline 2304c16 (covers `e2e/**` only). Migrated selectors find the same live thing under pixel markup: `section.hero` → `section.px-hero-block`, `#about` → `#profile`, `#process` → `#method`, `button[name="Toggle color theme"]` → `button[name="Switch between day and night"]`, `article.work-detail-body` → `article.px-work-body`, `.work-index-item` → `.px-work-index-item`; preserved every pre-existing assertion verbatim and its semantic strength, changing only the locator. When pixel removed semantics, retained original selector/assertion and documented the regression: (a) nav removed `<ul>/<li>` — `getByRole('list')`/`listitem` now document missing list semantics; (b) Writing nav `href="/writing/"` no longer hash-navigates to `#writing` (section `#writing` itself remains); (c) ClusterBid home reel absent — home Work is `a.px-matter` MatterRows with no `svg[data-reel-slug]` or `#case-*` ids; (d) hero `.hero`/`.wordmark`/`.site-nav`/`.nav-links`/`.nav-meta`/`hero-marquee`/`marquee-track`/`dl.case-spec`/`#case-*`/`#companion`/`data-wanderer-*`/`scene-frame` removed for `px-*` pixel islands; (e) `#about`/`#process` anchors renamed, `#experience` is additive and intentionally not asserted — loops stay individual `toBeVisible()` per id, not `toHaveCount`. Sanitized `e2e/content-negotiation.spec.ts` comments/titles to mechanism-neutral (no `proxy.ts`/`agent-proxy.ts`/`worker/index.ts`/matcher/rewrite) while keeping Link discovery (`GET /` Link rels) and both Markdown patterns (`/work/<slug>.md`, `/writing/<slug>.md`, `Accept: text/markdown`) assertions. Added `e2e/status.spec.ts`: real 404 via `GET /this-route-does-not-exist-404` (expects 404) and `page.goto` asserting `h1` “Page not found.”, copy “This page does not exist.”, decorative `canvas.px-status-field[aria-hidden="true"]`, and `a[href="/"]` “Go home” (focus asserts `toBeFocused` + `toBeInViewport` only — visible `:focus-visible` ring is global and not claimed); error boundary covered by precise `test.fixme` noting `app/error.tsx` (“Something failed.” / “This page could not be rendered.” / `Try again` + `Go home`) has no deterministic production throw path in current source and that `fixme` documents the missing trigger without proving execution — no test-only route, no invented throwing route, no false-positive negative control. Resolves prior medium findings: removed unauthorized extra home-card visibility assertion, kept loops as individual presence checks with `#experience` untested, narrowed focus/emoji/fixme claims to observable properties.
