- 2026-08-23 — Audited `next-mdx-remote` as dead weight. Runtime never imports it — `components/work/CaseStudyPage.tsx` and `app/writing/[slug]/page.tsx` now render prebuilt `lib/mdx/generated` via `getMdxModule` — so the only blockers are the type-only `MDXRemote` edge in `lib/mdx-options.ts:4-7` and the `next-mdx-remote/serialize` + `MDX_OPTIONS` test harness in `lib/mdx-options.test.ts`. Plan: make `scripts/build-mdx-modules.ts:48-55` the single plugin owner (or extract a neutral `lib/mdx/plugins.ts`), retarget the Shiki dual-theme/figure/plain-fence test to `compile` from `@mdx-js/mdx@3.1.1` (already the build compiler) without losing the `data-rehype-pretty-code-figure` / `--shiki-light` / `--shiki-dark` / `github-light github-dark-dimmed` contract, then delete `next-mdx-remote@6.0.0` from `package.json:46` + regenerate `pnpm-lock.yaml` (importer + `next-mdx-remote@6.0.0` package/snapshot) and drop `lib/mdx-options.ts` from `vitest.config.ts:46` coverage exclusion; doc staleness (`README.md:18`, primers, CSS comments) follows separately. Sequenced test-first then manifest, with focused proof (`tsc --noEmit`, `vitest` on the retargeted test, `pnpm mdx:modules` + grep for shiki vars in `*.mdx.js`, `pretypecheck`/`pretest` regeneration).
