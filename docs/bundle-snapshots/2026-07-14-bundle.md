# Bundle snapshot — 2026-07-14

## Scope and evidence boundary

This is a **local production-mode** measurement of a source snapshot copied from the live dirty checkout. It is not a clean-commit benchmark and is not evidence from Vercel or `https://akaushik.org`.

- Source `HEAD`: `b4e227880f187913449a2ecbcbcfd815aefee7c1`
- Capture time: `2026-07-14T13:31:25Z`
- Initial checkout inventory: 63 modified tracked paths and 10 untracked paths before these owned docs were authored
- Snapshot source set: 332 tracked or untracked, non-ignored entries
- Isolated build root: `/private/tmp/akaushik-bundle-20260714.8Jlf8c`
- Runtime: Node 22.23.1; pnpm 11.9.0
- Framework: Next.js 16.2.10 production Turbopack build

The temp copy prevented `pnpm build` and its generating `prebuild` hook from writing into the shared checkout. After integration tightened Wanderer to the pose-driven home route, the shared checkout was rebuilt under Node 22 and the HTTP/chunk collector was repeated at `2026-07-14T14:06:53Z`. The route/chunk tables below use that final integrated artifact; the isolated-build ledger remains the reproducible baseline for the measurement method.

## Command and exit ledger

### Runtime preflight

```sh
zsh -lc 'source "$HOME/.nvm/nvm.sh" && nvm version 22 && nvm exec 22 node --version && nvm exec 22 pnpm --version'
```

Exit `0`: NVM selected `v22.23.1`; pnpm reported `11.9.0`. The interactive shell itself was Node 24.16.0, so the explicit Node 22 selection was load-bearing.

### Dirty-source capture

```sh
tmp=$(mktemp -d /private/tmp/akaushik-bundle-20260714.XXXXXX)
git ls-files -co --exclude-standard -z |
  rsync -a --from0 --files-from=- ./ "$tmp/"
```

Exit `0`: `/private/tmp/akaushik-bundle-20260714.8Jlf8c`, 332 source entries at capture.

### Isolated dependencies

The first attempt linked the checkout's `node_modules` into the temp root and invoked the build:

```sh
PATH=/Users/abhishek/.nvm/versions/node/v22.23.1/bin:$PATH /usr/bin/time -p pnpm build
```

Exit `1` before compilation: `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`. This was a temp-environment setup failure, not a Next.js build result. The link was removed and the frozen lockfile was installed from the local pnpm store:

```sh
CI=true PATH=/Users/abhishek/.nvm/versions/node/v22.23.1/bin:$PATH \
  /usr/bin/time -p pnpm install --offline --frozen-lockfile
```

Exit `0`: 611 packages reused, 0 downloaded; `real 15.67` seconds.

### Production build

```sh
PATH=/Users/abhishek/.nvm/versions/node/v22.23.1/bin:$PATH \
  /usr/bin/time -p pnpm build
```

Exit `0`:

- `prebuild`: `agent-skills: digests up-to-date.`
- Next.js 16.2.10 Turbopack compiled successfully in 2.2 seconds.
- TypeScript finished in 2.2 seconds.
- 63 static outputs were generated in 1.768 seconds.
- Total wall time: `real 13.37` seconds.

The build classified `/`, `/work`, `/work/[slug]`, `/writing`, and `/writing/[slug]` as `ƒ` dynamic routes. A read-only artifact check found only `.next/server/app/_global-error.html`; the measured routes have no static route-HTML file.

### Local production server

```sh
PORT=3014 PATH=/Users/abhishek/.nvm/versions/node/v22.23.1/bin:$PATH pnpm start
```

The server became ready in 973 ms. It was deliberately stopped with `Ctrl-C` after measurement, so the final process exit was `130`; all HTTP collectors below exited `0` before shutdown.

## Route HTML and declared scripts

The collector requested identity HTML, five gzip HTML responses, and every local script URL present in each route's `<script src>`. Script sizes are response-body bytes; HTTP headers are excluded.

| Route        | Status | Identity HTML | Gzip samples                                 | Gzip median | Declared scripts | Declared-script gzip bodies |
| ------------ | -----: | ------------: | -------------------------------------------- | ----------: | ---------------: | --------------------------: |
| `/`          |    200 |     111,119 B | 31,712 / 31,730 / 31,738 / 31,740 / 31,770 B |    31,738 B |               11 |                   201,557 B |
| `/work/neev` |    200 |      47,316 B | 15,906 / 15,910 / 15,915 / 15,915 / 15,921 B |    15,915 B |               11 |                   195,427 B |
| `/writing`   |    200 |      36,101 B | 11,309 / 11,317 / 11,318 / 11,321 / 11,325 B |    11,318 B |               10 |                   194,821 B |

The gzip variation is expected: each dynamically-rendered response contains a different same-length nonce.

The home route declared these local scripts:

```text
/_next/static/chunks/0-xiibcvccke_.js
/_next/static/chunks/09w2mqrw_1-ho.js
/_next/static/chunks/0cz1d0mv5g_q7.js
/_next/static/chunks/0lz7y8ah-d6ru.js
/_next/static/chunks/14kpvfbzhc-kt.js
/_next/static/chunks/2_51k5if51uy2.js
/_next/static/chunks/2b65_a2g2vzi0.js
/_next/static/chunks/2eu2d3g2qp7it.js
/_next/static/chunks/2jxncqfah5m3f.js
/_next/static/chunks/turbopack-0dmkamcdbb8ja.js
/init-theme.js
```

Their raw artifact/body total is 667,846 bytes. With `Accept-Encoding: gzip`, the ten `.next` scripts totaled 200,682 response-body bytes; `/init-theme.js` was served as 875 identity bytes, producing the 201,557-byte declared-script total.

## Scene lazy chunks

The route HTML did **not** contain the three scene filenames below. Module inspection mapped `three`, AgentGraph, and Wanderer to separate runtime chunks:

| Role                    | Emitted chunk      |       Raw | Gzip response body | Brotli-11 artifact estimate |
| ----------------------- | ------------------ | --------: | -----------------: | --------------------------: |
| Shared raw `three`      | `20f4yvdace1wu.js` | 548,509 B |          136,119 B |                   110,875 B |
| AgentGraph component    | `1gif3a8mzrq5u.js` |   6,172 B |            2,662 B |                     2,320 B |
| Wanderer component      | `13q3w6g7o-d-e.js` |   5,725 B |            2,497 B |                     2,155 B |
| All three, counted once | —                  | 560,406 B |          141,278 B |                   115,350 B |

Interpretation:

- Home normal-motion desktop composition: 201,557 declared + 141,278 scene = **342,835 B (334.8 KiB)**.
- Home normal-motion below 861 px: Wanderer stays gated, so 201,557 + 136,119 + 2,662 = **340,338 B (332.4 KiB)**.
- `/work/neev` composition: **195,427 B (190.8 KiB)**; the home-only policy loads no scene chunk.
- `/writing` composition: **194,821 B (190.3 KiB)**; the home-only policy loads no scene chunk.

These are static compositions from measured response bodies and inspected runtime gates, not observed browser-waterfall or Lighthouse totals.

### Honest Wanderer delta

- Attributable Wanderer component chunk: **5,725 raw bytes / 2,497 gzip response-body bytes**.
- Incremental home scene payload after AgentGraph has loaded shared `three`: **2,497 gzip bytes**.
- Hypothetical first-scene payload if a later policy enables Wanderer without AgentGraph: **138,616 gzip bytes** for shared `three` plus Wanderer. Current non-home routes load neither.

No no-Wanderer counterfactual build was run. The reinstatement's total route delta cannot be inferred from 2,497 bytes because the client wrapper shares an initial chunk and the SVG fallback contributes HTML.

## Local runtime diagnostic

Five warm loopback requests on the same Node 22 server produced these TTFB medians:

| Route        | Samples                               |  Median |
| ------------ | ------------------------------------- | ------: |
| `/`          | 51.2 / 81.7 / 98.6 / 137.0 / 393.9 ms | 98.6 ms |
| `/work/neev` | 14.1 / 28.5 / 29.1 / 165.4 / 259.5 ms | 29.1 ms |
| `/writing`   | 5.4 / 12.6 / 47.2 / 72.8 / 190.8 ms   | 47.2 ms |

Loopback timing is a host diagnostic only. It does not prove the 2,500 ms deployed-production monitor threshold.

## What was not measured

- No `pnpm analyze` / webpack analyzer run was started.
- No Lighthouse run was started; the historical 299,026-byte Lighthouse result is not reused as current evidence.
- No browser waterfall was captured.
- No deployed Vercel runtime, production TTFB, CDN compression, third-party analytics, or public URL was tested.
- No counterfactual no-Wanderer build was run.

The accepted budget and operating procedure are in [ADR-0016](../adr/0016-post-launch-runtime-and-bundle-budget.md) and [BUNDLE_BUDGET.md](../BUNDLE_BUDGET.md).
