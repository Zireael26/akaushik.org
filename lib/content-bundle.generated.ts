// GENERATED by scripts/build-content-bundle.ts — do not edit.
// Run `pnpm content:bundle` (or any `pnpm build`) to regenerate.
//
// Keys are `<content type>/<slug>`; values are the raw MDX including its
// frontmatter fence. See the generator for why the content is inlined rather
// than read from disk at request time.

export const CONTENT_BUNDLE: Record<string, string> = {
  "case-studies/bluehost-agents": "---\ntitle: Bluehost · agents framework\ndek: The foundational platform behind Bluehost's agentic AI products.\nindex: \"03\"\ntag: Production scale\nyear: 2025 — now\nrole: Platform engineer · ongoing\nstack: [Agent runtime, tool-calling, observability]\nevidenceOf: Operating at scale · team context\n---\n\n# Bluehost · agents framework\n\n> The foundational platform behind Bluehost's agentic AI products.\n\nEmployer work under scope review. The shape of the contribution — a hand in maintaining and continuously improving the foundational platform behind Bluehost's agentic AI products — is real; the specifics are under confidentiality. Available to discuss in conversation: [hello@akaushik.org](mailto:hello@akaushik.org).\n",
  "case-studies/clusterbid": "---\ntitle: ClusterBid\ndek: Taking a Go-and-Next.js inference-cloud platform from monorepo checks to a real-host k3s UAT — without mistaking pre-production proof for production.\nindex: '05'\ntag: UAT platform engineering\nyear: 2026 — UAT\nrole: Engineering advisor · process & platform\nstack: [Go workspace, Next.js, TypeScript, Helm, k3s]\nevidenceOf: Pre-production delivery · platform discipline\ndraft: false\n---\n\n# ClusterBid\n\n> Taking a Go-and-Next.js inference-cloud platform from monorepo checks to a real-host k3s UAT, without mistaking pre-production proof for production.\n\n## Context\n\nClusterBid is a pre-production, region-pinned inference-cloud platform. Its repository puts Go services and shared packages, Next.js clients, TypeScript packages, Helm charts, and infrastructure code under one review surface. That shape is useful: a gateway change can move with its console contract and deployment values. It also creates a harder standard for what “green” means.\n\nMy role was engineering advisor across process and platform. The work here was not to claim a commercial launch. It was to make the repository and its deployment path tell the truth about what had been implemented, what had been exercised, and what was still mocked.\n\n## Problem\n\nA root command can look successful while checking only one part of a mixed repository. A Go workspace cannot honestly be covered by an undifferentiated `go vet ./...` at the root; each module needs its own invocation. The Next.js clients and TypeScript packages carry their own build, lint, and test contracts. A quality gate that treats those trees as one language will either miss work or become too noisy to trust.\n\nLocal verification was only half the problem. The first real-host UAT deploy surfaced assumptions that local and rendered-chart checks had not: stateful database containers could not start with a blanket capability drop, isolated image builds needed complete per-service module sums, and the console's browser-origin guard needed the environment's own public URL at runtime. Passing tests was necessary. Feeding deployment findings back into source was the real closure condition.\n\n## Approach\n\nThe fix had four layers.\n\n**Makefile as the routing layer.** The root `Makefile` enumerates the Go workspace modules and routes vet, lint, race tests, and coverage through each module. Separate TypeScript targets build, lint, and test the client and package trees. `quality-gate-local` composes those checks with repository linting, so one command can be broad without pretending every subtree uses the same tool.\n\n**Native git-boundary checks.** The repository has no root application package, so `.githooks/commit-msg` and `.githooks/pre-push` carry the conventional-commit and Trellis process-gate contracts without making a nested Next.js client own repository-wide policy. The pre-push hook blocks direct main pushes and delegates the actual verification to the same gate used elsewhere.\n\n**UAT as a separate contract.** A dedicated Helm overlay and bring-up runbook describe the single-VM k3s shape explicitly: one-node NATS settings, environment-specific public URLs, mock inference, local Stripe, and the secrets the deployment does and does not require. The UAT hosts use publicly valid TLS, but the workload remains a pre-production environment.\n\n**Deployment findings returned to source.** The chart, image-build, and runtime-origin fixes were persisted rather than left as one-off cluster patches. That makes the next render and upgrade reproduce the tested shape instead of depending on operator memory.\n\n## What reached UAT\n\nA real-host k3s UAT exposed the marketing site, console, and API gateway over TLS. A mock-inference request exercised the gateway, metering, and billing path, with spend and reconciliation checks observed in that environment. The exercise also produced durable source fixes for the chart and console runtime configuration.\n\nThat is meaningful engineering evidence, but it is deliberately narrower than a production result. The inference runner was in mock mode, Stripe used the in-repository local provider, and the fine-tuning storage path remained outside the UAT scope.\n\n## Trade-offs\n\n**One repository over service-by-service repositories.** Cross-surface changes stay reviewable together, at the cost of maintaining an explicit routing layer for validation.\n\n**Mock providers inside UAT.** Mock inference and local Stripe let the team test service topology, request flow, metering, billing, and browser contracts without pretending GPU or payment-provider readiness. The trade is intentional: topology confidence now, provider proof later.\n\n**Single-node UAT over production-shaped high availability.** The environment was sized to expose integration and deployment defects economically. Replica and storage decisions in that overlay are UAT constraints, not production recommendations.\n\n## Honest scope\n\nThis case study is about UAT and pre-production engineering. It makes no claim of customer traffic, adoption, revenue, a production deployment, live GPU inference, or live Stripe processing. The role claim is limited to engineering advisor for process and platform, and the technical claims stop at repository evidence plus the recorded UAT verification.\n",
  "case-studies/curat-money": "---\ntitle: curat.money\ndek: A fair-comparison tool for crypto cards, built like a real product.\nindex: \"04\"\ntag: Product breadth\nyear: 2026 — now\nrole: CTO · Tech Lead\nstack: [K8s, RBAC, CI/CD, High-throughput data pipeline]\nevidenceOf: Data pipeline to web product\n---\n\n# curat.money\n\n> A fair-comparison tool for crypto cards, built like a real product.\n\n## Context\n\nConsumer comparison sites for niche financial products tend to fail in the same direction: they publish data that is shallow, stale, and optimized for affiliate conversion rather than accuracy. The problem is structural. Provider data is scattered across marketing pages with inconsistent vocabulary, buried disclosures, and a strong incentive to make trade-offs opaque. A comparison tool that doesn't account for this isn't neutral. It inherits the provider's framing.\n\ncurat.money is built on a different premise: that a rigorous, source-checked comparison platform, with custody status made explicit and country coverage reflected honestly, is worth more than yet another shallow aggregator. The product serves people evaluating crypto cards — cards that allow holders to spend against a crypto balance or crypto collateral. The engineering problem is how to keep the data accurate and the platform operationally real at a scale where manual curation breaks down.\n\n## Problem\n\nCard data is spread across provider sites that each use their own vocabulary and their own level of willingness to surface trade-offs. Users need to filter by country availability, custody model, supported assets, fees, and rewards. They need to trust the filter. A comparison that silently lags a provider update or omits a custody distinction is worse than no comparison, because it gives false confidence.\n\nBeyond the data quality problem, the platform has to be operationally real: multi-environment hygiene so local development doesn't corrupt production state, a K8s deployment that matches where the product is heading rather than where it started, role-based access so internal operators and end users see the right surfaces, and a build pipeline that doesn't rot between runs.\n\n## Approach\n\nThe data spine is a scrape-normalize-verify loop. `scrape_and_update_prod.py` pulls provider data on a repeatable schedule; `match_cards.py` normalizes the raw results to a canonical schema; `custody_scrape_results_local.json` captures the local scrape state used for development and diff verification. Custody status is not derived from marketing copy. It is checked explicitly before a record is marked production-ready. The pipeline is designed to be re-run cleanly, so a failed scrape degrades gracefully rather than publishing partial data.\n\nMulti-role RBAC gates what internal operators, external contributors, and end users can do. This matters more than it sounds: a comparison platform that accepts external data contributions without access control is one bad merge away from compromised trust signals.\n\nThe production deployment runs on Kubernetes. `cloudbuild-web.yaml` drives the Cloud Build CI/CD path. The build is observable and auditable. It is not a one-off shell script that only the person who wrote it knows how to run. Developer experience is organized with Make-based targets: the kind of DX investment that reduces cognitive overhead across months of iteration, not just the first week.\n\n## What shipped\n\nA data pipeline producing normalized, verified card records from live provider sources. A multi-environment web product with role-based access across internal and public surfaces. A production K8s deployment with a Cloud Build pipeline and environment parity between local and production. The comparison table reflects custody status, country coverage, and card features from a source-checked record set rather than manually maintained copy.\n\n## Trade-offs\n\nProduct rigor over content-farm velocity: fewer listings, each defensible, rather than a large catalog of unverified entries. The comparison is only valuable if users can trust the filter. K8s over simpler hosting: this is deliberate over-engineering for a v0, chosen because the deployment model should match where the product is heading, not minimize the setup cost at launch.\n\n## Honest scope\n\nLive status, exact ownership boundaries, and whether to publish product metrics: Abhishek will confirm these before this section is finalized. Some readers will bring strong priors about the asset class; the case study's claim is about product engineering discipline and data pipeline rigor, not about the category itself.\n",
  "case-studies/neev": "---\ntitle: Neev\ndek: Bringing AI to an industry that still runs on WhatsApp.\nindex: \"01\"\ntag: Hero case · MSME thesis\nyear: 2026 — now\nrole: Co-founder & CTO — product, architecture, build\nstack: [Next.js, Postgres, multi-tenant monolith]\nevidenceOf: MSME depth · systems & product discipline\n---\n\n# Neev\n\n> Bringing AI to an industry that still runs on WhatsApp.\n\n## Context\n\nIndia has roughly 63 million MSMEs — 6.34 crore, per the National Sample Survey Office's 2017 [Key Indicators of Unincorporated Non-Agricultural Enterprises](https://www.mospi.gov.in/sites/default/files/press_release/press_noteNSS_KI_73_2.34_29jun17.pdf). Most of them (textile traders, regional distributors, small manufacturing units) operate on WhatsApp conversations, paper ledgers, a Tally install, and a few shared spreadsheets. This isn't backwardness. It's a pragmatic equilibrium built on trust relationships, cash-flow timing, and a low tolerance for software that doesn't survive contact with the day.\n\nAny AI product aimed at this audience has to earn its place under that daily reality, not above it. That means the platform has to be useful before it's smart, and the smart parts have to be invisible enough that an operator who has never touched AI before doesn't have to think about them.\n\n## Problem\n\nDistributors in the textile supply chain lose time and money at very specific seams: order capture (still mostly over WhatsApp), ledger reconciliation (still mostly by hand), GST-compliant invoicing (patchy), and memory of who owes whom what (fragile, relationship-dependent). Generic ERPs don't fit because they assume a workflow that doesn't exist in this business yet. Pure chatbot layers don't fit because they solve a symptom, not the operations below it.\n\nThe actual problem is building a platform with enough structural honesty that a real MSME can adopt it incrementally, then layering AI in where it removes friction rather than demanding new behavior.\n\n## Approach\n\nThe architecture reflects the operating environment, not a tech-stack wishlist. Four principles shaped it.\n\nIt is a modular monolith with multi-tenant discipline: one deployable, clean module boundaries, and `tenant_id` enforced at every persistence boundary. That trades early micro-service flexibility for operational sanity at the scale MSMEs actually operate at. The wrong distributed system on a two-person team costs far more than modularizing later does.\n\nTextile distribution is the beachhead, not the ceiling. The data models are built so the platform generalizes to adjacent MSME verticals, and the first release narrows to where there is direct operator input and real feedback.\n\nAI goes where it reduces typing and memory load, not where it replaces judgment. Order capture, reconciliation assistance, and narrative summarization run over the operator's own ledger. Nothing decides autonomously. The operator stays in control of the outcome.\n\nThe engineering process is the proof of intent: PRD, ADRs, roadmap, changelog, process-gate scripts. Not process theatre. Process that earns trust from a technical buyer who looks under the hood.\n\n## What shipped\n\nThe first deployable version of the Neev platform is live. It ships a multi-tenant foundation with strict `tenant_id` invariants throughout the persistence layer, core domain models for the textile distribution workflow (orders, ledger, parties, items), and the first AI-assisted surfaces in the operator workflow. Public engineering artifacts (README, PRD, architectural decision records) are available alongside the build.\n\nThe stack is Next.js on the front end against a Postgres-backed monolith, with Neev's own process-gate scripts keeping the repo honest across builds.\n\n## Trade-offs\n\n**Monolith over microservices.** Deliberately. A small team debugging a distributed tracing problem while onboarding a first paying customer is a bad place to be. The module boundaries are clean; the leap to separate services, if it ever makes sense, is a refactor, not a rebuild.\n\n**Vertical-first over horizontal-first.** Textile distribution is the wedge. A horizontal \"MSME platform\" from day one would have drifted into genericism and served no vertical well. The constraint is the product.\n\n**Owner-operator UX over admin-console UX.** The primary user sits on a chair in a shop, often on a phone. Desktop-dense dashboards aren't the frame. The UI is built around how a distributor actually moves through a working day.\n\n## Honest scope\n\n> The engineering artifacts are real and reviewable. The operator validation is in progress. That's the honest state of a system built correctly from the start, not retrofitted after the fact.\n\n## What I'd do next\n\nThree things, in the order they'd earn their keep. Vernacular-language input for WhatsApp order capture, because the operators who most need this are not typing in English. An offline-first mobile app, because a distributor's phone loses signal in a warehouse and the ledger cannot wait for it to come back. Then a reconciliation assistant as the first full agent surface, with audit-grade output. That is the first place where the system does real work unsupervised, which is exactly why it has to show what it did.\n",
  "case-studies/vericite": "---\ntitle: VeriCite\ndek: A retrieval stack an institution can actually trust with its own words.\nindex: \"02\"\ntag: AI systems depth\nyear: 2026 — now\nrole: Co-founder & CTO — retrieval pipeline\nstack: [HF TEI, Qdrant, Ory, Kubernetes, Vercel]\nevidenceOf: Institutional AI-systems sophistication\n---\n\n# VeriCite\n\n> A retrieval stack an institution can actually trust with its own words.\n\n## Context\n\nInstitutional RAG is a different problem from consumer RAG. A consumer chatbot can miss the right passage and the user shrugs and rephrases. An institution can't. When a knowledge system surfaces a passage, the institution will be asked why that passage appeared: by an auditor, a regulator, or a faculty committee. The answer has to hold. The embedding model, the reranker, the chunking strategy, the tenancy model — every layer is load-bearing in a way it simply isn't when the stakes are a search result.\n\nVeriCite is a multi-tenant institutional RAG platform. The product surface runs on Vercel. The retrieval pipeline behind it has to earn the trust that the product surface asks for.\n\n## Problem\n\nI started the pipeline on Fastembed running ONNX models on CPU. That's a reasonable place to start: low infrastructure overhead, quick to iterate, and good enough to validate the retrieval idea. But it imposed real limits that compounded as the product matured.\n\nFirst, the embedding-model catalog available through Fastembed's ONNX path is narrower than what HF TEI can serve. I needed `paraphrase-multilingual-MiniLM-L12-v2` as the primary embedder and `BAAI/bge-reranker-v2-m3` as the cross-encoder reranker. Both are worth running correctly. The throughput curve on CPU-ONNX gets expensive before it gets good. Second, batching behavior and GPU-aware scheduling are first-class in TEI; bolting them onto a CPU-ONNX path is the wrong direction. Third, the system needed to stay defensibly multi-tenant from the start, with identity enforced at the query path, not added as a filter at read time.\n\n## Approach\n\nI moved embedding and reranking to Hugging Face Text Embeddings Inference (TEI). TEI is purpose-built for serving transformer-based embedders and rerankers: it handles dynamic batching, GPU allocation, and model-specific optimizations that Fastembed on CPU can't match. The move to TEI wasn't a drop-in swap. I re-validated retrieval quality on the institution's corpus, reconciled output format differences, and updated the upstream chunking pipeline to feed TEI's batch interface correctly.\n\nI separated the embedding and reranking paths. `paraphrase-multilingual-MiniLM-L12-v2` answers the first question: is this chunk plausibly relevant? `BAAI/bge-reranker-v2-m3` answers the harder one: of the plausibly relevant chunks, which ones actually are? Keeping these as independent services means each can scale, be swapped, or be versioned without destabilizing the other.\n\nQdrant is the vector store. Per-tenant filtering is enforced at the retrieval layer via Qdrant's payload filters, not added on top afterward. This is the correct place to enforce tenancy: letting vectors cross tenant boundaries at retrieval and filtering later is a design flaw, not a trade-off.\n\nI used Ory for identity. Institutional tenants arrive with non-trivial auth requirements: SSO, provisioned users, role hierarchies. Ory handles that surface area properly. A lightweight JWT library would have required rebuilding it.\n\nThe codebase is a disciplined monorepo with four top-level namespaces: `apps/` for product surfaces, `packages/` for shared retrieval libraries and utilities, `infra/` for Terraform and cloud configuration, and `k8s/` for Kubernetes manifests. This layout lets the serving infrastructure, the retrieval library, and the Vercel-deployed product surface evolve on independent cadences without accumulating drift between them.\n\n## What shipped\n\n- Production-ready institutional RAG stack running `paraphrase-multilingual-MiniLM-L12-v2` for embedding and `BAAI/bge-reranker-v2-m3` for reranking, served via HF TEI.\n- Multi-tenant retrieval with tenant-scoped Qdrant collections and payload-level filters enforced at query time.\n- Identity layer built on Ory, covering SSO and provisioned-user workflows for institutional customers.\n- Monorepo with CI/CD pipelines, Kubernetes manifests under `k8s/`, and the product web surface deployed on Vercel.\n- Completed migration from Fastembed ONNX to TEI, including corpus re-validation and batch pipeline updates.\n\n*(Abhishek: confirm current deployment status and any institution-specific retrieval quality numbers you can share publicly.)*\n\n## Trade-offs\n\n**TEI over Fastembed.** I chose more infrastructure to operate — a running TEI service with GPU access rather than an in-process ONNX runtime. The payoff is a wider model catalog, correct batching behavior, and a throughput curve that doesn't break before you want it to. For a consumer prototype, Fastembed is fine. For an institutional product where retrieval quality is the core claim, the infrastructure cost is the right trade.\n\n**Qdrant over a Postgres vector extension.** I chose a purpose-built vector store, which adds an extra service to operate and monitor. The operational overhead is real. What it buys is a retrieval layer designed for the access patterns a RAG pipeline actually has — payload filtering, named collections, and a query API that doesn't require thinking around a general-purpose database's constraints.\n\n**Ory over a lightweight auth library.** The surface area is larger and the learning curve is steeper. Institutional identity requirements (provisioned users, SSO, org-level RBAC) make that surface area necessary, not incidental. A lightweight library would have deferred the problem, not solved it.\n\n## Honest scope\n\n*(Abhishek: confirm how much of this is your individual design/implementation vs. team/contracted work; list explicitly what's yours to claim.)*\n",
  "writing/_test-draft": "---\ntitle: Test draft\ndek: hidden\ndate: 2026-05-19\ndraft: true\nart: operations\n---\n\n# test\n",
  "writing/ai-for-msme": "---\ntitle: Notes on bringing AI to an MSME\ndek: WhatsApp, paper ledgers, Tally, and a few spreadsheets. What actually moves the needle, and what doesn't.\ndate: 2026-03-20\nart: field-work\n---\n\n# Notes on bringing AI to an MSME\n\n> WhatsApp, paper ledgers, Tally, and a few spreadsheets. What actually moves the needle, and what doesn't.\n\n## A textile distributor's paper stack is a deliberate choice\n\nThe textile distributor I spent time with last year wasn't running his business badly. He was running it the way it works. WhatsApp threads with buyers and suppliers. Paper ledgers for stock. Tally for the accountant. A few shared spreadsheets for the delivery schedule. Every part of that stack is a decision that has already worked well enough to survive. Any pitch for AI has to reckon with that.\n\n## Why replacing an MSME's existing system fails\n\nThe most common mistake in bringing AI to a business like this is treating the existing system as a problem to be replaced. It isn't. It's a working equilibrium built on trust, cash-flow timing, and muscle memory. An AI that demands new behaviour in exchange for its benefits won't get adopted. The cost of the change is immediate and concrete; the benefit is distant and abstract.\n\n## Which AI applications earn their place in a distributor's workflow\n\nWhat does earn its place is anything that removes typing, removes memory load, or closes the gap between what happened and what got recorded. Three applications clear that bar today.\n\n### Voice and photo order capture\n\nA distributor can speak an order or photograph it, and the AI does the transcription and structuring. The typing goes away and the record still gets made.\n\n### Reconciliation across the ledger and the WhatsApp thread\n\nReconciliation help is for the operator who keeps stock in a paper ledger and confirms it over WhatsApp: here's what the ledger says, here's what the WhatsApp thread says, here are the two discrepancies.\n\n### Summarisation over the operator's own data\n\nSummarisation runs over the operator's own ledgers, orders, and threads rather than over some generic corpus. What it answers are questions about this business, not questions about businesses like it.\n\n## Which decisions AI should not automate for an MSME\n\nWhat doesn't earn its place yet is autonomous decisioning on anything contested. When a buyer disputes a quantity or a supplier adjusts an invoice the resolution is relational, not informational. The operator has to be in that conversation. An AI trying to close it will get turned off.\n\n## How Neev applies this\n\nNeev is where this thinking is going. A modular operations platform for MSMEs, starting with textile distribution. The design constraint is that nothing in the workflow should require the operator to learn a new mental model. Harder than it sounds.\n",
  "writing/best-practices-into-trellis": "---\ntitle: \"Folding Anthropic's large-codebase playbook into Trellis\"\ndek: A point-by-point gap analysis against the latest Claude Code best-practices guide, and the ten changes I shipped to close the gaps.\ndate: 2026-05-18\nart: agent-process\n---\n\n# Folding Anthropic's large-codebase playbook into Trellis\n\n> A point-by-point gap analysis against the latest Claude Code best-practices guide, and the ten changes I shipped to close the gaps.\n\nAnthropic just published [How Claude Code works in large codebases](https://claude.com/blog/how-claude-code-works-in-large-codebases-best-practices-and-where-to-start). It is the most direct guide I have seen them write about how to actually wire Claude Code up at the project level: `CLAUDE.md` shape, hooks, skills, MCP, LSP, governance, the whole stack.\n\nTrellis is the regime I run my six personal projects under. One canonical control plane, project-level inheritance through symlinks, three tiers of hooks, ten weekly audits. I [wrote about it last week](/writing/trellis). The Anthropic post landed seven days later and I read it the way you read a competitor's deck, looking for what they thought was load-bearing that I had missed.\n\nThis post is the gap analysis and the resulting PR. If you run Claude Code at any scale, the practical question is the same one I asked: what did the people who built Claude Code think mattered, and how much of it does your setup already do?\n\nFor Trellis the score is \"most of it.\" The places it does not are interesting, and they are what shipped today as [v0.3.0](https://github.com/Zireael26/trellis-instance/pull/63).\n\n---\n\n## The map: what Anthropic said, what Trellis already did, what was missing\n\nI read the post once for argument, twice for receipts. Every recommendation that named a file path, an env var, a config key, or a workflow shape got pulled into a table next to its Trellis counterpart.\n\nWhat Trellis already had:\n\n- Layered `CLAUDE.md`: root `core-rules/CLAUDE.md` plus per-project files, both inherited through symlinks. Target size for each is under 5 KB, which matches the guide's \"pointers and gotchas only\" framing.\n- Hooks for self-improving setup: eight canonical hooks across Claude Code and Codex, six events. Stop hooks gate Definition of Done; pre-push catches direct-to-main; PreToolUse blocks destructive commands.\n- Skills loaded on demand: seven canonical (`process-gate`, `security-gate`, the spec-kit pipeline of `clarify` → `spec` → `plan` → `tasks` → `analyze`).\n- Subagent dispatch: `core-rules/CLAUDE.md` mandates parallel sub-agents for independent units, and the spec-kit pipeline is a sequential split with checkpoints.\n- Code-review parity: every edit-heavy turn dispatches a code-review subagent against the diff. Findings are advisory but visible.\n- Governance: a registry, a blacklist, a deferred list for n=1 rule candidates, version pins, weekly audits that scan for drift. One DRI (me), explicit by construction.\n- Self-improving rules: `gotchas.md` per project, plus a monthly `gotchas-rollup` audit that clusters across the fleet and promotes patterns hitting n=3 into parent rules.\n\nWhat Trellis did not have, ranked by impact:\n\n1. **Token-noise filter.** The Anthropic post is unambiguous: version-control a `permissions.deny` block that excludes generated files, build artifacts, third-party code. Trellis's canonical `claude-settings.json` shipped no `permissions` block at all. Every project's agent could read `node_modules`, `.next`, `dist`, `vendor`, lockfiles, every cache directory. Wasted context on every session.\n2. **Per-subdirectory scoping for test and lint.** The guide specifically calls this out for service-oriented codebases. Trellis's `stop-verify.sh` ran every check at repo root regardless of where the agent edited. On clusterbid-console, with five Go services, a Next frontend, and a Python tree, a single-file edit would type-check and lint the entire monorepo and routinely blow past the 90-second budget.\n3. **Codebase map.** The post recommends \"one-line descriptions of top-level folders as a table of contents\" for big repos. Trellis project `CLAUDE.md` templates seeded no such section. With nine top-level dirs in some projects, the agent re-discovered the tree on every fresh session.\n4. **Configuration aging.** \"Review CLAUDE.md every 3-6 months as models evolve.\" Trellis had `version-drift`, `dep-currency`, `bypass-tripwire`, `parent-hook-drift`, `cross-project-process-audit`. None of them asked the question \"is this rule obsolete now?\"\n5. **LSP / symbol-level navigation.** Zero integration anywhere in Trellis. Polyglot projects pay this cost most.\n6. **Skill path-scoping.** Every Trellis skill loads globally. Fine for workflow skills (process-gate, security-gate). Wrong shape for stack-specific helpers a polyglot monorepo might want.\n7. **The read-only explorer pattern.** \"Read-only subagent maps subsystem, writes findings to file. Main agent edits with full picture.\" Trellis had `/primer` for stable subsystems, but no quick ephemeral counterpart for one-shot exploration.\n8. **Stop hook proposing rules.** The guide explicitly suggests Stop hooks that propose `CLAUDE.md` updates while the session is fresh. Trellis's `gotchas.md` mechanism relied on the agent volunteering entries. No hook actually scanned the just-finished session for patterns worth keeping.\n9. **MCP allowlist.** Solo-DRI projects do not need a governance committee, but they do benefit from an explicit list of which MCP servers are sanctioned. The Anthropic post calls this out; Trellis had nothing in the schema.\n10. **Plugin marketplace.** Already on Trellis's roadmap; not in scope for this PR.\n\nThe work that ships today closes the first nine. Plugin marketplace stays parked.\n\n---\n\n## Three phases, one PR\n\nThe Trellis PR-size hard cap is 800 lines. The bundle was 852. Splitting into three PRs is exactly the kind of false-economy split the cap is meant to prevent. Phase 1B (the codebase-map convention) is the rule that phase 2F (the audit) enforces; phase 3G, 3I, and 3J all share the same gitignore-fragment sentinel; the `core-rules/VERSION` bump is per release, not per change. Each split PR would land in a state that fails a different audit until the next one landed on top of it.\n\nSo the PR carries a single-page ADR explaining the carve-out, and ships as one.\n\n### Phase 1: quick wins\n\n**`permissions.deny` baseline.** The canonical settings template now ships an explicit deny list covering `node_modules`, `.next`, `dist`, `build`, `out`, `target`, `vendor`, `.venv`, `__pycache__`, every cache directory, and every lockfile I could name. New projects pick it up through `onboard-project.sh`. Existing projects converge through a new `scripts/rollout-settings.sh` that jq-unions canonical + project-local entries and preserves anything the project added by hand. Idempotent. The single cheapest change in the bundle, and the one that saves the most tokens.\n\n**Mandatory codebase map.** Project `CLAUDE.md` now requires a `## Codebase map` section when the project has five or more top-level directories. Below that threshold the section is noise; the agent can hold them in head from one `ls`. Above it, the cost of re-discovery on every fresh session adds up enough to justify five lines of upfront context. The format is bare: one line per directory, role only. If a directory needs more, write a sibling `docs/<dir>.md` and link to it from the map line.\n\n**`obsolete-rules` audit.** A new quarterly audit walks the entire rules corpus: parent `CLAUDE.md`, presets, `engineering-process.md`, every project `CLAUDE.md`, and every `gotchas.md`. It classifies each rule as load-bearing, compensating for the model or agent client, stylistic, or process. Load-bearing, stylistic, and process rules stay. Compensation rules are candidates. A strong removal proposal needs all of these: no `gotchas.md` evidence in the last six months, written before the current major model generation, and no enforcement hook still depending on it. Weak proposals carry the inconclusive signals.\n\nThis is the only Trellis audit that proposes *removals* rather than additions. The bar is set high on purpose. A wrong removal silently degrades discipline; a missed removal just wastes a few tokens. The first run after a major model launch will be the highest-yield, because that is when pre-launch workarounds become candidates.\n\n### Phase 2: monorepo scoping\n\n**Subtree-aware `stop-verify`.** When every changed file in a Stop turn sits under one subdirectory that carries its own manifest (`package.json`, `go.mod`, `pyproject.toml`, or `Cargo.toml`), the hook now `cd`s into that subtree before typecheck, lint, and test. Mixed-subtree changes fall back to repo root. Escape hatch: `PROCESS_GATE_FORCE_ROOT=1` runs at root unconditionally.\n\nThe detection is conservative on purpose. Requires every changed file to share one subtree, and that subtree must already carry a manifest. Single-language repos behave identically to before. The win shows up on polyglot monorepos: clusterbid-console's typecheck-on-Stop dropped from \"run every Go module + Next + Python\" to \"run only the affected subtree\" without me having to write a single new validator. Patched in both the Claude Code and Codex parity copies of the script.\n\n**Optional skill scope.** A new `scope.json` convention next to a project-local `SKILL.md` lets the project declare which paths the skill auto-fires under. Schema is tiny:\n\n```json\n{\n  \"paths\": [\"services/**\", \"pkg/**\"],\n  \"reason\": \"Go-only validators; would noise the Next.js subtree\"\n}\n```\n\nCanonical skills stay global. Every canonical Trellis skill is workflow-shaped, not stack-shaped. The scoping is for project-local skills that a polyglot monorepo might want to keep silent on the wrong subtree. Explicit `/skill <name>` invocations always work regardless of scope; only auto-mention is gated.\n\nThis one is agent-followed, not engine-enforced. Claude Code does not natively read `scope.json`. Every project loads `core-rules/CLAUDE.md`, which now directs the agent to respect the file when present. If `n=2` projects find the convention load-bearing, the next move is a `parent-hook-drift` audit row that validates `scope.json` schema across the fleet.\n\n**Codebase-map enforcement.** The weekly `cross-project-process-audit` got a new check. Projects with five or more top-level directories must carry a `## Codebase map` heading. Three failure modes classified separately: missing heading (warning), heading present but stub-only (info), heading references directories that no longer exist (warning). Sub-threshold projects skip the check entirely. The rule existed on paper after phase 1B; this commit makes it mechanically enforced.\n\n### Phase 3: subagent, LSP, governance\n\n**`/explore` slash command.** Trellis already had a `/primer` system for capturing durable context. The Anthropic post calls out a different shape: a *just-in-time*, *transient* exploration that the editing session loads once and discards. So `/explore` is the ephemeral counterpart. It dispatches a read-only subagent to map the subsystem, writes the result to `<canonical-root>/.claude/primers/_explore/<slug>-<sha>.md`, and the editing session reads it before touching code. Post-edit, the user decides whether to promote to `/primer` or delete.\n\nThe directory shape reuses the primer infrastructure deliberately. One canonical-root resolution path. One set of conventions. One audit cycle to keep both honest. The `_explore/` subdir is not loaded at session start; only durable primers in `INDEX.md` are. Leftover notes are harmless but accumulate. Periodic cleanup is the user's call.\n\n**LSP recommendation.** A new section in `engineering-process.md` §8.3 recommends installing per-language LSP binaries (`gopls`, `typescript-language-server`, `pyright`, `rust-analyzer`) plus a Claude Code LSP plugin for repos spanning two or more languages. Recommendation only, no audit. Single-language repos derive less value. The win is on polyglot monorepos where the same identifier exists in three languages and ripgrep returns forty false positives but symbol lookup returns the right one.\n\n**`propose-rules` Stop hook.** Experimental, opt-in. Activated by setting `PROCESS_GATE_PROPOSE_RULES=1` in the project's hook config. Three layers of cost control before any subagent fires:\n\n1. **Opt-in gate.** Unset means silent exit. Every project gets the file through `sync-hooks`, but nobody pays tokens unless they flipped the switch.\n2. **Pure-chat guard.** No file edits this turn means nothing to learn from.\n3. **Transcript-tail heuristic.** Subagent only fires when the last two hundred transcript lines contain at least one explicit-correction signal: \"no\", \"don't\", \"actually\", \"stop doing\", \"that's wrong\", \"never do\". If the user did not correct anything, the hook spends nothing.\n\nWhen it does fire, a one-shot `claude -p --max-turns 1` subagent reads the transcript tail and the project's `gotchas.md` and proposes a single candidate entry, or emits the literal string `NONE`. The proposal returns as `additionalContext`. The hook never blocks. The decision to append is the user's.\n\nThis pairs with the existing monthly `gotchas-rollup`. `propose-rules` surfaces n=1 candidates per turn; `gotchas-rollup` clusters n=3 candidates into parent-rule promotions. The time horizons differ. The self-improvement loop is the same.\n\n**`approved_mcps` schema field.** Optional, documentation-only today. The trellis config schema accepts an array of `{ name, purpose, scope: \"fleet\" | \"per-project\" }` entries. Solo-DRI projects do not need a governance committee, but the explicit allowlist makes the fleet's sanctioned MCP set readable in one place. The example config seeds the three I actually wire: `scheduled-tasks` for the audits, `computer-use` for native-app verification, `claude-in-chrome` for browser flows. A parked `mcp-drift` audit will pick this up when `n=2` projects diverge in what they connect to.\n\n---\n\n## What I noticed along the way\n\nA few things that did not become commits but are worth saying out loud.\n\nThe Anthropic post is more prescriptive than I expected. Not in a bad way. \"Review CLAUDE.md every 3-6 months\" is a cadence I would not have set on my own, and the framing of \"rules that compensated for limitations the model no longer has\" is the most useful new lens I have picked up in months. Trellis grew a quarterly audit specifically because of that one sentence.\n\nMost of the recommendations Trellis already covered were ones I had reverse-engineered from incidents. Force-push to main happened once; pre-push guard happened the next day. Agent summarised work it did not do; receipts-required entered the parent rules. The Anthropic post lists those as best practices because Anthropic has watched many teams hit the same incidents. They are not novel insights. They are the consensus shape that emerges from enough operational pain across enough teams. Trellis hit them faster because it is a six-project regime, not a one-project regime. Every project is one Rule-of-Three candidate.\n\nWhat was novel: `permissions.deny`. I had not thought of generated-directory exclusion as a *Trellis* concern; it felt like project hygiene. The post correctly frames it as a token-noise filter that belongs in the canonical layer because every project has the same `node_modules` problem.\n\nAnd the subtree-scoping change in `stop-verify` was the largest single quality-of-life upgrade in months. Going from \"every Stop turn runs every check across every language in the monorepo\" to \"runs the right scope automatically\" is the difference between Trellis being useful on polyglot repos and Trellis being a tax on them.\n\n---\n\n## The PR\n\n[github.com/Zireael26/trellis-instance/pull/63](https://github.com/Zireael26/trellis-instance/pull/63). Twelve commits, one ADR, `core-rules/VERSION` 0.2.0 → 0.3.0. Each phase is its own commit so the bundle is reviewable in passes. The ADR at [`docs/adr/2026-05-18-anthropic-best-practices-bundled-rollout.md`](https://github.com/Zireael26/trellis-instance/pull/63/files) carries the size carve-out and explains why splitting harms clarity.\n\nThe public template repo will follow on the next `sync-to-template.sh` run. Downstream consumers see the new version via `scripts/upgrade.sh` once the canonical clone tags `v0.3.0`.\n\nRe-reading the Anthropic post and asking what Trellis still does not do is now the same exercise as the quarterly `obsolete-rules` audit, just in the other direction: additions instead of removals. The next major Claude or Codex release is when the audit earns its keep. I am looking forward to running it.\n",
  "writing/building-this-portfolio": "---\ntitle: Building this portfolio\ndek: Why the site ships with the same process I'd bring to a client engagement. PRD, ADRs, ROADMAP, process-gate, agent-readiness, the whole thing in the open.\ndate: 2026-04-21\nart: site-craft\n---\n\n# Building this portfolio\n\n> Why the site ships with the same process I'd bring to a client engagement. PRD, ADRs, ROADMAP, process-gate, agent-readiness, the whole thing in the open.\n\nA portfolio is supposed to show the work. The trouble is that *showing* the work usually means showing the surface: the typography, the case studies, the tagline. The decisions underneath are what matter, and they don't fit in a screenshot.\n\nSo this one is built differently. Same process I'd use on a paid client engagement, left in public view, every decision and trade-off legible to anyone who reads the repo.\n\n## The receipts\n\n- [`docs/PRD.md`](https://github.com/Zireael26/akaushik.org/blob/main/docs/PRD.md): product requirements. What the site has to do, who it's for, what a good reading experience looks like for the MSME owner *and* the senior engineer.\n- [`docs/ROADMAP.md`](https://github.com/Zireael26/akaushik.org/blob/main/docs/ROADMAP.md): phased delivery plan. Six phases from scaffold to launch; each phase is a set of one-PR slices.\n- [`docs/adr/`](https://github.com/Zireael26/akaushik.org/tree/main/docs/adr): architecture decisions, numbered and dated. Seven so far. Each captures what was decided, what was considered, and why.\n- [`docs/AGENT_READINESS.md`](https://github.com/Zireael26/akaushik.org/blob/main/docs/AGENT_READINESS.md): the contract for agent crawlers. llms.txt, sitemap, RFC 8288 Link headers, content negotiation on `Accept: text/markdown`, api-catalog, Agent Skills. Implementation in Phase 4.\n- [`docs/CHANGELOG.md`](https://github.com/Zireael26/akaushik.org/blob/main/docs/CHANGELOG.md): every shipped change, categorised, referencing the PR that landed it.\n- [`scripts/process-gate.mjs`](https://github.com/Zireael26/akaushik.org/blob/main/scripts/process-gate.mjs): the pre-commit hook that refuses to let me commit code without a CHANGELOG entry (R1), structural changes without an ADR (R2), or epic changes without a ROADMAP update (R3). It ran on every one of the 30+ PRs that built the site.\n\n## Why this way\n\nI'm writing for two readers.\n\nThe **MSME owner** I might work with on Neev wants clarity. Can this person explain what they've built? Will it survive contact with the day? The home page and the case studies answer that, in plain English, with honest scope on what worked and what didn't.\n\nThe **senior engineer** I might work with on a platform team wants rigour. How do they handle change? How do they treat deprecations? Are they the kind of person who ships and forgets, or the kind who leaves a trail? The repo answers that.\n\nBoth readers can get what they came for without the other's material getting in the way. The portfolio is the entry point; everything under `docs/` is the depth behind it.\n\n## The parts that were load-bearing\n\n**Next.js 16 over SvelteKit** (ADR-0001). The Next + Turbopack + R3F ecosystem is where the Vercel-adjacent toolchain lives, and the Phase-4 agent-readiness work needed Route Handlers and middleware at the shape Next provides. SvelteKit is a fine framework. This wasn't the site for it.\n\n**Process-gate as pre-commit** (ADR-0002). Three rules: code needs a CHANGELOG entry, structural changes need an ADR, epic-scale changes need a ROADMAP update. A shell script on `simple-git-hooks` enforces them. Cheap to write, load-bearing against drift.\n\n**MDX with server-only compilation and Shiki bundle isolation** (ADR-0004). `next-mdx-remote@6` compiles MDX inside React Server Components; Shiki never reaches the client bundle. `pnpm analyze` re-verifies the isolation every PR.\n\n**Content negotiation, Pattern A and Pattern B** (ADR-0006). Every page has a `.md` alternate at `/page.md` (Pattern B, load-bearing) and responds to `Accept: text/markdown` at the canonical URL (Pattern A, additive). Passes isitagentready.com's content-negotiation check on both axes and loses neither if one misbehaves.\n\n**The Wanderer crane port** (slice 5.1c). 221 lines of vanilla Three.js in the reference design, ported directly into a `useEffect`-driven scene rather than R3F: the crane is a fixed-position full-document scene driven by document scroll, and wrapping that through R3F primitives reads worse than the direct port. Eight named POSES, IntersectionObserver-driven pose dispatch, damp lerp, scroll-velocity rotation, wing flap, pointer parallax. Bail-out to SVG fallback if the first frame takes more than 80 ms.\n\n## What's still to do\n\nLaunch isn't done. It's done enough to ship. The open follow-ups live in `docs/ROADMAP.md`: the honest list, with items like \"tighten the JS bundle back toward 150 KiB once the bundle-analyzer audit identifies which preloaded chunk is blowing past the target,\" \"write a real `/api/docs` page,\" and \"run the isitagentready.com scan against prod and persist the screenshot to `docs/agent-readiness-snapshots/`.\"\n\nNone of those are in the \"shouldn't launch without this\" pile. They're the work that comes after launch, which is where most real engagements live anyway.\n\n## If any of this looks useful\n\nEvery file I've referenced is in the public repo. Clone it, read it, take what's useful. If you're starting a portfolio or a product and want the same process on it: [hello@akaushik.org](mailto:hello@akaushik.org).\n",
  "writing/detection-is-not-continuity": "---\ntitle: \"Detection is not continuity\"\ndek: Three failures on one machine made the same point: a system that notices it is broken and then stops has only relocated the failure.\ndate: 2026-07-26\nunlisted: true\nart: operations\n---\n\n# Detection is not continuity\n\n> Three failures on one machine made the same point: a system that notices it is broken and then stops has only relocated the failure.\n\nThis morning, a local router told me it had handled 1,416 requests with zero errors. At the same time, agents behind it were losing responses mid-stream.\n\nThe number was accurate. That was the problem.\n\nThe same session turned up two more versions of that mistake in the [process layer I run around coding agents](/writing/trellis-1-0-rc). A disk janitor could identify a clean, pushed worktree and still delete it while somebody was working inside. The obvious fallback for a failed model lane would keep agents moving by silently spending a different quota pool. Each system could see enough to claim health or recovery. None could carry the work through without moving the damage somewhere less visible.\n\n---\n\n## Zero meant exactly what the counter said\n\nThe router split requests across two model lanes. Its `errors` counter incremented on two events: an HTTP response at 400 or above, or a socket error. That sounds broad until the upstream returns 200, starts a valid stream, and then sends an in-band SSE error frame.\n\nThat request satisfied the counter's definition of success. The HTTP status was 200. The socket had not failed. When the client received the error frame and closed the connection, the router counted the close as `aborted`, which assigned ownership to the client.\n\nAn upstream failure became a successful request followed by a badly behaved client.\n\nSo the dashboard could report 1,416 requests and zero errors while streams were dying. An earlier version of the same bug had reported 102 requests and zero errors while responses were being truncated. I had two clean summaries from two broken systems because both summaries watched the handshake and ignored what happened inside the stream.\n\nThe fix starts with naming the real event. An in-band upstream error is an upstream failure even when the HTTP envelope says 200. The upstream frame caused the later client disconnect. The router has to carry that distinction into its counters and logs or the next investigation begins with evidence that points in the wrong direction. A counter is evidence only about its increment condition.\n\n---\n\n## The directory was clean and occupied\n\nThe disk janitor had a more physical version of the same hole. It reaped old worktrees after checking their git state: the tree had to be clean, then either merged or pushed. That protected uncommitted work and branches that existed only on one machine. Nothing in the gate checked whether a running process had its current directory inside the tree.\n\n`docs/adr/2026-07-16-scheduled-safe-worktree-reap.md` had already described the exact case. A clean, pushed, unmerged tree can still be the working directory of a live unit. The document chose merged-ness as a deliberate proxy for \"nobody is in there\" and left live-process scanning out as slower, macOS-specific machinery.\n\nThe prediction sat in the repository for ten days. Then a 1.0 GB worktree passed every deletion gate while five processes had their current directory inside it.\n\nGit state answered recoverability. The gate was silent about whether the work was still in progress, which left the person using the directory outside the safety model.\n\nThe repair lives in `scripts/lib/disk-janitor-lib.sh`. One host-wide `lsof -n -P -Fn` snapshot per scan finds current directories and open handles under every candidate tree. On this machine it costs 0.21 seconds. The command runs under a five-second alarm, and any missing tool, timeout, empty result, or nonzero exit blocks deletion. Then the janitor takes a fresh snapshot immediately before each individual removal, because a process can enter a tree after the plan is built and before the action runs.\n\n---\n\n## The fallback that would have hidden the outage\n\nWhen the foreign lane goes down, the obvious repair is server-side substitution. Route the request to the first-party model, preserve the API shape, and let the running agent continue.\n\nThat would improve availability and make the system less honest.\n\nThe two lanes exist partly to separate quota pools. A silent fallback spends the first-party subscription without telling the caller, the logs, or the operator. The request appears recovered. The first visible symptom arrives later as a rate limit in an unrelated session, with no direct trail back to the outage that consumed the quota.\n\nI did not have to speculate about that failure. Four agents typed for the foreign lane turned out to be running the first-party model. A spawn-time parameter overrode the agent definition, and those four agents produced every first-party rate limit I saw that day. The system had already run the accidental version of silent fallback. It looked like continuity at the request boundary and like random quota exhaustion everywhere else.\n\nPromoting that accident into router policy would make the bug harder to detect and impossible for the caller to control.\n\nThe degradation belongs at the caller. The router should report that the foreign lane is unavailable. The caller can then stop, retry later, reduce the unit, or choose the first-party model under an explicit policy. Whatever it chooses has to be visible and logged. Crossing a model or quota boundary is a decision, not a transport detail.\n\n---\n\n## The negative result belongs in the same ledger\n\nThe same discipline has to work in the other direction. A bug report claimed that Codex rollout files were writing hard enough to damage SSDs. The claim sounded plausible because the files were large and growing.\n\nI checked the write pattern and the rate. The files are append-only. They grew by 215 MB per day. Against a 300 TBW drive, that is 0.026 percent of the rated write endurance per year.\n\nIt does not reproduce.\n\nThe files may still be an occupancy problem. Across rollouts larger than 5 MB, base64-encoded PNGs accounted for 38.4 percent of the bytes. But occupancy is not wear. Treating the frightening claim as true would have produced a fix for the wrong problem, just as treating zero errors as health delayed the fix for a real one.\n\n---\n\n## What I got wrong\n\nI trusted the word `errors` before reading the predicate beneath it. The counter answered a narrower question than I assumed. The class fix is to define operational metrics by the events they count and inspect the blind spots before using the headline number as evidence.\n\nI accepted a proxy after the design had already named its failure mode. Merged or pushed was useful git evidence and silent about occupancy. The ten-day gap between prediction and incident was not a discovery period. It was an implementation debt with a countdown.\n\nAnd I nearly treated invisible substitution as resilience. The quota split made that fallback a transfer of cost and failure ownership. The completed request hid the cost, which surfaced later as a rate limit in an unrelated session.\n\n---\n\n## The next failure should leave better evidence\n\nI now read operational counters by their predicates and inspect who owns an ambiguous disconnect. Destructive action gets a live-state check at the moment it fires; a fallback that crosses a model or quota boundary stays with the caller and leaves a log entry.\n\nI will know this is working when the next lane outage leaves a visible degraded receipt and no surprise first-party rate limit, and when the next janitor scan skips a clean but occupied tree.\n",
  "writing/fastembed-to-tei": "---\ntitle: Migrating from Fastembed ONNX to Hugging Face TEI\ndek: The specific trade-offs, the retrieval quality delta on an institutional corpus, and why the infra complexity was worth it.\ndate: 2026-02-18\nart: retrieval\n---\n\n# Migrating from Fastembed ONNX to Hugging Face TEI\n\n> The specific trade-offs, the retrieval quality delta on an institutional corpus, and why the infra complexity was worth it.\n\nFastembed ONNX on CPU is a reasonable starting point for a RAG system. Easy to install, model catalog covers the obvious choices, and you can have a working embedding pipeline in an afternoon without standing up extra infrastructure. For VeriCite's early proof-of-concept it was the right call.\n\nIt stopped being the right call when retrieval quality became the constraint. The model catalog that is practical to serve through Fastembed doesn't include `paraphrase-multilingual-MiniLM-L12-v2` at the throughput we needed on an institutional corpus, and it doesn't include a cross-encoder reranker like `BAAI/bge-reranker-v2-m3` at all. Reranking matters disproportionately on the kind of corpus VeriCite deals with: documents where lexical overlap between a query and the relevant passage is low, and a bi-encoder's cosine similarity is a noisy signal.\n\nHugging Face TEI gives you both. Purpose-built inference server for text embeddings and rerankers, batching tuned for GPU throughput. The cost is operational: GPU nodes to run, batching configuration to manage, k8s manifests to ship for a TEI sidecar alongside Qdrant. Real infra complexity, and it doesn't pay for itself unless retrieval quality is actually on the critical path.\n\nIn this case it was. Retrieval quality on the institutional corpus improved measurably after the migration, most visibly on queries whose vocabulary diverged from the relevant document's. That is the problem the cross-encoder solves: it sees the full (query, passage) pair instead of comparing independent embeddings. Bi-encoder for the neighbourhood, reranker for the door.\n\nInfra overhead is now a fixed cost. The model options going forward are wide open.\n",
  "writing/gptx-in-trellis": "---\ntitle: 'GPTx: putting GPT agents inside Claude Code'\ndek: Claude Code is where I want the work to run. GPTx keeps Claude in charge while selected GPT workers join through the same agent interface, inside the same project session.\ndate: 2026-07-27\nart: agent-process\n---\n\n# GPTx: putting GPT agents inside Claude Code\n\n> Claude Code is where I want the work to run. GPTx keeps Claude in charge while selected GPT workers join through the same agent interface, inside the same project session.\n\nI primarily use Claude Code. The reason is larger than which model wins a benchmark in a given week. It gives me the better working environment: one main loop that can plan, delegate, review, run tools, hold a long project session, start named teammates, and compose those agents into workflows.\n\nI also have a Codex subscription and want GPT agents for the units where they are useful. Before GPTx, those two preferences pulled in opposite directions. I could stay in Claude Code and launch Codex as a child process with a frozen work order, or replace the whole Claude Code session with GPT and lose the Claude-pinned parts of the agent system.\n\nGPTx removes that choice. I start Claude Code normally. Claude remains the main loop. When a unit suits GPT, Claude starts `gpt-mid` or `gpt-sol` through the ordinary agent interface. The worker joins the same project session, uses the same worktree, and reports back like any other agent.\n\nThat is the feature.\n\n---\n\n## Trellis is the control plane, not the session\n\nThe distinction matters here. [Trellis](/writing/trellis) is the framework I use to manage the engineering process across my projects. Its control plane owns the parent rules, hooks, skills, routing doctrine, project registry, merge gates, and scheduled audits. Registered projects inherit that layer.\n\nThe sessions still belong to Claude Code or Codex, running inside those projects. There is no separate Trellis runtime and no such thing as a Trellis session. A precise description of GPTx is: GPT agents running inside a Claude Code session for a Trellis-managed project.\n\nGPTx itself is installed at user scope because it has to work from every project, whether I start Claude Code in a terminal, an IDE, cmux, or headless mode. Trellis supplies the policy around that availability. The machine can expose a GPT worker everywhere without every task deserving one.\n\nAvailability is not adoption. I measured that early: installing the agents was not enough to make a fresh orchestrator choose them. The routing doctrine is the part that turns a model name into an operating policy.\n\n---\n\n## The model is selected per agent\n\nThe older `claudex` path pointed an entire Claude Code process at a GPT model. The main loop became GPT, every unpinned subagent followed it, and features pinned to Sonnet or Opus failed because the proxy only understood the other lane. Cross-model review could quietly turn into GPT reviewing GPT.\n\nGPTx routes each request by its model name instead. Claude model ids pass straight through to Anthropic. A closed list of GPT and Codex ids goes to a local Codex proxy. Unknown model names stay on the Claude lane.\n\nThat last default is load-bearing. Claude Code starts internal agents and may introduce aliases I have not seen yet. An unknown name should not change providers because a regular expression was generous.\n\nThe Claude side is byte-for-byte passthrough. Its subscription OAuth, prompt-cache controls, beta headers, long-context flag, and effort setting are not translated into another provider's request shape. The Claude credential never enters the Codex proxy. Only requests explicitly naming a configured GPT model cross the other lane.\n\nThere is no GPTx command to remember. I run `claude` in the project as usual. The model pinned in an agent definition selects the lane.\n\n---\n\n## GPT workers get the Claude Code agent surface\n\nThe earlier Codex executor in Trellis was useful, but it was a relay. Claude wrote a bounded work order, launched another process, waited for files and a receipt, then reviewed the result. Each call began from the handoff.\n\nA GPTx worker is an ordinary Claude Code agent. Its definition controls its tools and permissions. It can read the repository, edit files, run tests, return structured output, and participate in parallel workflows. It works against the same git state as the orchestrator because both are inside the same project.\n\nIt can also be named. In the acceptance run, Claude started a GPT teammate and a Claude teammate concurrently. After the GPT worker finished its first unit, Claude sent it another message. Claude Code resumed the same agent id, with the worker's earlier context intact, instead of constructing another process and hoping the next prompt carried enough history.\n\nThe workflow path needed no special adapter. A three-agent fan-out selected `gpt-sol` by agent type, required a schema, and returned three schema-valid objects. The router log showed Claude handling orchestration and synthesis while GPT handled the delegated turns between them.\n\nClaude Code does the coordination. GPTx only decides where a particular agent request goes.\n\n---\n\n## Trellis decides which GPT worker to use\n\nBoth GPT agents currently run GPT-5.6 Sol. The difference is effort and the kind of proof surrounding the unit.\n\n`gpt-mid` runs at medium effort. I use it for bounded implementation with a strong oracle: a failing test, type checker, schema, compiler, or deterministic reproduction. It also gets mechanical volume, where the specification is already exact and more search would buy latency rather than confidence.\n\n`gpt-sol` runs at xhigh effort. It gets the units where several plausible answers survive, the oracle is weak, or a wrong decision is expensive to unwind. Root-cause work that survived an initial pass belongs here. Security-sensitive analysis does too.\n\nImportance is not the routing predicate. A large rename with a compiler is still medium. A twelve-line authorization change with no useful test can justify xhigh.\n\nI measured the effort ladder before writing the doctrine. On a one-line fix and a mechanical extraction task, every effort level returned the same answer. On a design-judgement prompt, the deeper settings spent more tokens and time, although the result was noisy rather than monotonic. The rule that survived was simple: use the oracle when there is one; spend search when there is not.\n\nOutside dynamic workflows, those two agent definitions make the ladder deliberately two-valued. Workflows can also reach `high` directly. I do not keep cheap GPT agents below medium on this installation. Sending mechanical work to xhigh was the quota mistake I had to fix, not a lack of lower rungs.\n\n---\n\n## Review crosses the model boundary\n\nThe useful part of a second model family is the different failure surface. GPTx makes that explicit instead of treating another invocation as review.\n\n`gpt-sol-reviewer` is a read-only GPT agent for work written by Claude. It can inspect a diff and run checks, but it cannot edit. Its report has to identify a concrete failure with a file and line. If GPT wrote the work, this reviewer stops; another context window does not turn self-review into independence.\n\nThe reverse path is `opus-advisor`. Claude Code exposes its built-in advisor tool to GPT agents in the schema, then rejects it at dispatch. I tried the experimental flag, an explicit advisor model, and a GPT main loop. All three failed before an Anthropic request left the machine.\n\nSo the GPT workers can start one real, read-only Opus subagent instead. The transcript shows the question, the advice, and what the GPT worker did afterward. The advisor cannot edit, run shell commands, or delegate again.\n\nThat nested path required raising the process-wide spawn depth. The containment therefore lives in each agent definition. Workers receive the agent tool when they have a reason to consult Opus. The advisor does not.\n\n---\n\n## Context and operations are part of the feature\n\nClaude Code does not know the context window of a model absent from its catalog. In the first long GPT runs, it let an agent pass 188,000 tokens without compacting and the upstream eventually rejected the request. Compaction was working against the wrong ceiling.\n\nGPTx sets the effective ceiling to 160,000 tokens for these sessions. In the proof run, a GPT agent compacted once at 133,576 tokens, fell to 14,320, and completed the work. The largest accepted body at that point was 533,020 bytes, or 3.99 bytes per token. A 100,000-token ceiling was worse: normal multi-file work compacted repeatedly until the run died.\n\nThe operating surface is visible beside the session. The statusline distinguishes healthy, uncertified, cooling, degraded, dead-proxy, and dead-router states. A local page shows the lanes, request counts, in-flight work, recent failures, the largest GPT request, and the longest silence between stream chunks.\n\nClaude Code upgrades trip a certification flag when the client version or the observed union of beta headers changes. `gptx-doctor --certify` then runs five probes: router health, a real Claude request, a GPT completion, a GPT tool round trip, and GPT structured output. A spent Codex window gets its own exit code because unavailable quota is not proof of a broken upgrade.\n\nThe escape hatch is a settings override that sends a new session directly to Anthropic. Removing a shell variable does not work because Claude Code's settings environment wins. I tested the tempting command before documenting the useful one.\n\n---\n\n## One lane can fail without hiding the substitution\n\nGPTx keeps the Claude and Codex quota pools distinct. If the Codex proxy is unavailable, the GPT agent fails visibly while the Claude session remains usable. The router never rewrites the model to make the request appear successful.\n\nI have already seen the accidental version of that substitution. A spawn-time model override sent four supposed GPT workers to Claude. Those four agents accounted for every Claude rate limit in the session. The work completed, but the receipt named the wrong pool.\n\nSo degradation belongs to the caller. Trellis can rerun the same bounded unit on Claude after a GPT-lane failure, but the change is explicit, the work order stays identical, and the unavailable attempt remains in the record. A silent router fallback would spend the subscription GPTx was meant to preserve and call that continuity.\n\nThis also makes the failure boundary useful across projects. The model lane is an optional capability. Project work should still be possible on a machine without it, and an unavailable lane is a capability result rather than a completed unit.\n\n---\n\n## What broke after rollout\n\nThe first dashboard trusted the HTTP status and socket error events. An upstream could return 200, begin a valid event stream, then report an error inside that stream or end before its terminal event. The router recorded 102 requests and zero errors while agents were losing responses.\n\nIt also tried to be helpful after a stream had started by appending a JSON error object before closing the connection. That converted the original upstream failure into a malformed event stream.\n\nThe fix was narrower than the diagnosis. A mid-stream failure now destroys the connection so Claude Code sees a retryable shape. The router retries once only when no response byte reached the client. It owns and recycles its outbound sockets instead of relying on defaults designed for short HTTP calls. The counters now separate cuts, aborts, retries, upstream errors, and failures delivered inside a 200 response.\n\nThose bugs matter because they changed what I could trust on the operating surface. They are not the reason GPTx exists.\n\n---\n\n## The boundary is still private\n\nGPTx is instance-private today. Its router, agent definitions, doctor, and provider binding do not sync to the public Trellis template. GPT-5.6 Sol is the only GPT model I run through it, and the Codex OAuth translation path is unofficial.\n\nThe public template now carries the generic model-lane contract: explicit routing, a fail-closed capability probe, visible degradation, and no silent substitution. It does not carry my machine's proxy, ports, credentials, or process setup.\n\nThat is the honest boundary. I can start Claude Code in any registered project, keep Claude in the main loop, send a bounded unit to a GPT agent, resume that worker by name, require a schema, ask for a cross-model review, or let the GPT worker consult Opus. The transport underneath those operations is still mine to operate.\n\nI want the next month to be boring: mixed-agent receipts accumulating across real projects, effort decisions that leave both quota pools usable, and no dashboard claiming green while a worker disappears mid-stream. If the record says otherwise, the next change belongs in Trellis before it belongs in another article.\n",
  "writing/micrograd-makemore": "---\ntitle: What I learned building micrograd and makemore from scratch\ndek: A foundations-first reading of Karpathy's Zero to Hero, and why re-implementing the thing is the only way to understand the thing.\ndate: 2026-04-15\nart: ml-foundations\n---\n\n# What I learned building micrograd and makemore from scratch\n\n> A foundations-first reading of Karpathy's Zero to Hero, and why re-implementing the thing is the only way to understand the thing.\n\nYou can watch a video on a topic, nod along, feel the concept land, and move on. Or you can close the video and type the thing yourself. The two feel identical in the moment. They aren't.\n\nAndrej Karpathy's video course [Neural Networks: Zero to Hero](https://karpathy.ai/zero-to-hero.html) runs from scalar-valued autograd to a transformer. I did both passes: watched first, rebuilt second. The rebuild was slower, more frustrating, and the only pass that counted.\n\nMicrograd is the smaller project, a reverse-mode autodiff engine over scalars. Every value is a node, every op builds the graph, `backward()` walks the graph in topological order and accumulates gradients. The implementation fits in a few hundred lines. The understanding doesn't. Until you've traced a gradient through manual backprop yourself, \"PyTorch handles broadcasting\" stays an abstraction. It does handle it. You only own that fact after you've been the one handling it.\n\nMakemore extends the lesson: bigram counts, MLP, BatchNorm, WaveNet-style dilated convolutions, a character-level transformer. One new idea per step. The point isn't to implement them all at once but to hold the previous idea in your head while you add the next. The sequencing is the pedagogy.\n\nAfter building them I stopped treating gradient flow as magic substrate and started reading it like a data structure. That makes a real difference when you're debugging a production model that won't converge.\n\nThe rest of the writing on this site applies the same foundations-before-abstraction habit to production stacks. Agent frameworks, embedding pipelines, operational AI for businesses that haven't deployed it before.\n",
  "writing/native-git-hooks-for-non-node": "---\ntitle: \"Native git hooks for projects without a Node runtime\"\ndek: When husky is the wrong tool. Wiring pre-commit, commit-msg, and pre-push enforcement into a Unity game and a Go-first monorepo without inventing a package.json just to install lint-staged.\ndate: 2026-05-24\nart: agent-process\n---\n\n# Native git hooks for projects without a Node runtime\n\n> When husky is the wrong tool. Wiring pre-commit, commit-msg, and pre-push enforcement into a Unity game and a Go-first monorepo without inventing a package.json just to install lint-staged.\n\nTrellis has a git-boundary tier: pre-commit, commit-msg, and pre-push. On every Node project it leans on husky. Twice now husky has been the wrong fit. Lume is a Unity game; ClusterBid is a Go workspace with five services, a Next frontend, and a Python tree on the way. Both forced the same workaround, and the workaround is the post.\n\n---\n\n## The shape husky assumes\n\nHusky wants a `package.json`. It registers hooks through a `prepare` script that runs on `npm install`, and almost every project that ships husky also ships lint-staged for the \"only re-run on changed files\" half. That stack is the right answer when Node is already a first-class citizen of the repo.\n\nIt is the wrong answer with no meaningful root `package.json` to mount it on. Inventing a stub manifest whose only job is hosting husky buys you a dev-dependency lockfile to babysit, a `node_modules/` to gitignore, and a Node toolchain you do not otherwise need.\n\n---\n\n## The native alternative\n\nGit supports this directly. Set `core.hooksPath` to a tracked directory, commit shell scripts into it, and the same enforcement lives in plain `sh`:\n\n```sh\ngit config core.hooksPath .githooks\n```\n\nThe directory holds executable scripts named for the hook events, such as `.githooks/commit-msg` and `.githooks/pre-push`. Because they are tracked in git, enforcement is visible in repo state and survives any clone. The trade is one extra line in `README.md` for shedding the entire Node bootstrap.\n\nLume runs this against Unity. ClusterBid runs it with `make vet && make lint` inside the pre-push body, where the root `Makefile` fans out across the Go workspace, the Next frontend, and Python when it lands. No husky, no `prepare` script, no dev-dependency drift.\n\n---\n\n## Deferring pre-commit is not skipping discipline\n\nBoth projects ship `.githooks/commit-msg` and `.githooks/pre-push` and nothing else. The `pre-push` is non-negotiable. It carries the Trellis PR-flow guard that blocks direct pushes to `main`, with `TRELLIS_ALLOW_MAIN_PUSH=1` as the audited emergency override. That is the load-bearing hook in the tier.\n\n`pre-commit` is the slot I deliberately leave empty. The canonical Node profile runs lint-staged here; the Unity equivalent would be a validator I have one witness for, and the polyglot equivalent a Go-plus-Python-plus-Next composite I have one witness for. Rule of Three says n=1 is the danger zone. Until a second project demands the same shape, the slot stays empty.\n\nThe canonical six gates still run on every PR via the process-gate skill server-side. Local pre-commit is fast feedback, not enforcement. Deferring it slows the inner loop slightly; it does not weaken the regime.\n\n---\n\n## What you give up\n\nTwo real costs. First, lint-staged itself: the \"only the files I just touched\" filter is a few lines of shell, `git diff --cached --name-only --diff-filter=ACMR` piped through `grep` into the linter, but you do have to write it. Second, the `prepare`-script auto-install: a fresh clone needs the developer to set `core.hooksPath` once, and that line lives in the README rather than firing automatically.\n\nBoth are real friction. Both beat dragging a Node toolchain into a Unity project or a Go monorepo solely to host a hook runner.\n\n---\n\nThe companion piece this week, on stack profiles for the process-gate skill, covers the other half: how the canonical six gates adapt when the underlying stack is not a Node app. Together they are how Trellis enforces itself on projects that look nothing like a Node app.\n",
  "writing/process-gate-stack-profiles": "---\ntitle: \"Stack profiles: where the engineering process bends without breaking\"\ndek: How Lume (Unity) and ClusterBid (polyglot Go+TS+Python) each forced a new stack-profile carve-out, and what that taught me about the Rule of Three.\ndate: 2026-05-22\nart: agent-process\n---\n\n# Where the engineering process bends without breaking\n\n> How Lume (Unity) and ClusterBid (polyglot Go+TS+Python) each forced a new stack-profile carve-out, and what that taught me about the Rule of Three.\n\n## Six gates and a pressure valve\n\nI wrote about [Trellis](/writing/trellis) two weeks ago, the parent/child engineering-process regime that sits in front of every coding agent I run. The shortest version: one rulebook, nine hooks, ten weekly audits, and a `process-gate` skill that emits a verdict block before any PR can merge.\n\nThe skill runs six canonical gates. PR hygiene checks Conventional Commits, branch names, and size budgets. Secrets scans the diff. Bypass markers catches `--no-verify`, force-push, and the `TRELLIS_ALLOW_MAIN_PUSH=1` override. Tests and coverage runs the project's typecheck / lint / fast-test triad. Docs discipline confirms the change carries a CHANGELOG entry, an ADR if it should, and any required gotchas update.\n\nThat's five. The sixth is the stack profile. It is the only one that doesn't ship with a fixed implementation. The canonical layer says \"run the validators the project declares.\" The project says, in `local.config.sh`, what those validators are.\n\nFor most rules this asymmetry would be a smell. No secrets, no force-push to `main`, no marking a task done while tests fail. Those are universal, and a universal rule should have a universal implementation. But some checks are genuinely stack-shaped. Unity projects need to confirm every asset has a paired `.meta` file. Go workspaces need a `vet` loop that fans into each module because `go vet ./...` from the workspace root is unsupported. A Next.js portfolio site needs a design-token validator that wouldn't make sense anywhere else. The underlying constraint is different in each case, so one canonical script can't catch all three.\n\nThe stack-profile slot is the pressure valve. It lets the parent layer stay small without pretending the world is uniform.\n\n---\n\n## Lume: the first carve-out\n\nLume is a 3D Unity game I onboarded in late April. It's the first Trellis project with no Node runtime, no `package.json`, no pnpm anywhere in the tree. Husky doesn't apply because its whole bootstrap assumes a JS package manager. The hook stack had to land somewhere else.\n\nThe somewhere-else is native git hooks. `git config core.hooksPath = .githooks`, scripts under `.githooks/`, executable bit set. The canonical commit-msg validator and pre-push guard get copied in and lightly adapted, replacing the husky-flavored package-runner tail with direct shell calls. That part wasn't novel; native git hooks were already documented as the fallback for Rust, Go, and Python projects.\n\nWhat was novel was the stack profile. Lume declares it explicitly:\n\n```bash\n# .claude/skills/process-gate-local/local.config.sh\nPROCESS_GATE_STACK_PROFILE=\"unity\"\nPROCESS_GATE_STACK_VALIDATORS=(\n  \"scripts/check-meta-files.sh\"\n  \"scripts/check-asset-bundle.sh\"\n)\n```\n\nThe validators are project-local. Lume is the only Unity project I run, which means n=1, which means the Rule of Three says nothing canonical ships under `unity` yet. The reference under `core-rules/skills/process-gate/references/stack-profiles.md` documents the profile and lists the candidate validators, but the scripts themselves live inside Lume's tree, not in the parent.\n\nThe same constraint applies a layer down. `.githooks/` carries `commit-msg` and `pre-push` only. No `.githooks/pre-commit` until Unity-specific pre-commit validators reach Rule of Three (n≥2). The canonical six gates still run server-side via the process-gate skill on PR, so the gap doesn't weaken anything that ships. It just means I don't get to bake \"every commit must have a paired meta file\" into a pre-commit hook based on one project's preferences.\n\nThat restraint is the point. The first time I touched a Unity-shaped problem, the strong temptation was to stash canonical-looking scripts under `core-rules/`. I wrote them inside Lume instead. If a second Unity project never shows up, those scripts retire with Lume and nothing has to be cleaned out of the parent layer.\n\n---\n\n## ClusterBid: a polyglot does the same dance\n\nClusterBid Console onboarded a few weeks later. The shape is harder: Go workspace with five services plus three `pkg/*` libraries and a `tools/` tree, a Next.js frontend under `clients/` and `ts/`, and Python planned for GPU runners under `services/inference-runner` and `services/fine-tuning-orchestrator`. Three production languages, one repository, one CI surface, one process gate.\n\nNo root `package.json`, so husky is out. It uses the same fallback as Lume, `core.hooksPath = .githooks`. The interesting part is what `pre-push` does next. The Trellis PR-flow guard runs first, blocking direct push to `main` with the documented override. Then the hook calls `make vet && make lint`. The Makefile is the entire reason this works: it owns the language-specific fan-out, so the hook itself stays language-agnostic.\n\nThat fan-out exists because Go workspaces forbid `go vet ./...` from the workspace root. You have to `cd` into each module and run vet there. The naive translation of the JS triad would have meant rewriting the canonical `check-tests.sh` to know about Go workspaces. The Makefile pattern absorbs the complexity: `Makefile vet|lint|test` is the contract, and the per-language loops live inside the Makefile body. From the process-gate skill's perspective, ClusterBid looks like every other project. There is a typecheck command, a lint command, a test command; they exit with the right codes and the verdict block fills in.\n\nThe carve-out goes the same way as Lume's. `PROCESS_GATE_STACK_PROFILE=\"monorepo-polyglot\"` in `local.config.sh`, candidate validators sketched in the reference doc (`check-orchestrator-coverage.sh`, `check-proto-contract.sh`, `check-ci-subtree-routing.sh`), all implemented project-local. ClusterBid is the only polyglot monorepo I run, so n=1, so nothing promotes. `pre-commit` is deferred until polyglot-specific validators reach Rule of Three across at least three polyglot projects, almost word-for-word the same deferral Lume's row carries.\n\nThe two projects didn't coordinate. They arrived at the same answer because the shape of the rule forced it.\n\n---\n\n## The Rule of Three is doing real work\n\nBoth profiles sit at n=1 today. Lume is the sole witness of `unity`. ClusterBid is the sole witness of `monorepo-polyglot`. Neither one gets to promote validators into the canonical layer until a second project of the same shape shows up. Even then, the promotion doesn't happen at n=2. It happens at n=3.\n\nThat gap between n=2 and n=3 is where the rule earns its keep. With one project I have a hypothesis. With two, I have something that looks like a pattern but is statistically indistinguishable from coincidence. Two projects might share a quirk because of a single decision I made twice, a library I chose for both, or a deadline that pushed me toward the same shortcut. With three independent projects, the pattern has cleared the noise floor. Whatever's still common across all three is structural, not incidental.\n\nThe temptation to skip ahead is constant. After Lume I wanted to write a canonical Unity profile because the validators felt obvious. After ClusterBid I wanted to write a canonical polyglot profile because the Makefile orchestrator pattern is genuinely clean. Both times the right move was a thorough reference document and project-local scripts. The reference captures the shape so the second adopter doesn't have to re-derive it; the scripts stay local so a single-project guess doesn't lock in the wrong abstraction.\n\nThe system handles this by counting in the reference docs themselves. `references/stack-profiles.md` lists every profile with its current witness count and what's required to promote. `references/monorepo-polyglot.md` has a whole section on the promotion path: diff the validators between adopters, wait for the third witness, lift per the canonical procedure. Neither doc pretends canonical scripts exist yet for these profiles. The honesty is load-bearing; the moment a reference doc implies more canonization than actually exists, the deferral system erodes.\n\nWhat's still rough is the inverse direction. There's no codified test for when a profile should *graduate* from carve-out to canonical. n=3 is the threshold, but I haven't written the audit that flags candidates ready to promote. Today it's a manual review during quarterly housekeeping. The references themselves are uneven too: `monorepo-polyglot.md` is thorough; the Unity entry is thinner because Lume is still pre-MVP and the validators are speculative more than battle-tested. Both gaps resolve themselves the moment a real bug ships that the check would have caught. The doc updates after the receipt, not before.\n\nThe next stack profile to land, whatever shape it is, will be the third witness for something. That's the moment the system either confirms the Rule of Three was the right threshold or shows me where it was too strict. I'm betting on confirmation. I built the system so being wrong is cheap.\n",
  "writing/renaming-projects": "---\ntitle: \"Renaming six projects on the same day\"\ndek: A retro on the 2026-04-24 mass rename across directories, GitHub repos, hook configs, and scheduled-audit paths, plus the registry-first pattern that made the rest tractable.\ndate: 2026-05-26\nart: operations\n---\n\n# Renaming six projects on the same day\n\n> A retro on the 2026-04-24 mass rename across directories, GitHub repos, hook configs, and scheduled-audit paths, plus the registry-first pattern that made the rest tractable.\n\n## What got renamed and why\n\nOn 2026-04-24 I renamed four projects in one afternoon. `developerabhishek.live` became `akaushik.org` across the domain, GitHub repo, and on-disk directory. ADR-0003 carried the reason: a 22-character host that reads as \"dot live\" fails the dictation test, and the canonical URL is baked into too many manifests to flip later. `msme-neev` became `neev` because the MSME qualifier had stopped meaning anything inside the codebase. `institutional-rag-bot` became `vericite`. `swipe` became `curat.money`. The last two earned real product names.\n\nDifferent motivations, same week. The point isn't that you should rename your projects. The point is that several of them independently hit \"this name no longer fits\" within a few weeks of each other, and the day I sat down to do them all I learned what part of my setup tolerated renames and what part did not.\n\n---\n\n## The registry-first pattern\n\nThe only thing that kept the day from going sideways was doing the renames in the same order every time, starting with the registry.\n\nThe Trellis registry (`registry.md` in the control plane) is the single source of truth for \"what active projects exist.\" Every rename touched it first: name column, `Path`, and a `Notes` entry recording what the project used to be called. The on-disk directory move came next. The GitHub repo rename third. Everything else was a consequence.\n\nThe trick is that the scheduled `cross-project-process-audit` cron iterates over the registry. The moment the registry path changed, the next audit run flagged every project whose hooks, primers, or workspace configs still pointed at the old path. Drift surfaced itself within twenty-four hours instead of marinating until I tripped over it. The audit became the punch list.\n\nLesson, said plainly: if you have a single source of truth that something automated reads on a schedule, update the source of truth *first*. The schedule does the cleanup work for you.\n\n---\n\n## What actually broke\n\nThe friendly parts first. GitHub repo renames are transparent. GitHub keeps a redirect from the old URL forever, existing clones keep working, and `.git/config` auto-rewrites the remote URL on the next fetch. I had braced for pain there and got none.\n\nThe on-disk directory rename was where the day got expensive. Every absolute path written into a `CLAUDE.md`, every IDE workspace file, every shell alias, every `cd /Users/abhishek/projects/personal/<old>` muscle-memory broke at once.\n\nThree categories of breakage I didn't see coming:\n\n- Hook configs that hardcoded paths. A couple of project-local hooks had absolute paths to sibling repos for cross-project lookups. The failures were silent: the hook ran, found nothing, and returned clean.\n- Scheduled-task definitions. Any cron prompt that pinned `/personal/<old-name>/...` instead of resolving through the registry kept happily running against a directory that no longer existed.\n- Primer prose. Primer files are pinned to commit SHAs, which survived. But the prose inside several primers referenced files by old paths. `/primer-check` caught most; the rest I found when the next session loaded the primer and complained the file didn't exist.\n\nNone of this was hard in isolation. The cost was that each one surfaced one at a time, over the following week, in the middle of unrelated work.\n\n---\n\n## What I'd do differently\n\nTreat renames as a search-and-replace problem from the first minute. The strategy question, *should I rename this?*, gets all the air. The strategy is a five-minute decision and the search-and-replace is the actual day.\n\nWhat I want next time is a pre-flight script. Take the old name and the new name. Grep the entire control plane, every registered project, every scheduled-task definition, and every `.claude/` and `.codex/` tree. Dump the hit list. Group by file. That hit list *is* the migration plan. Most of it is mechanical sed; the rest is the small set of judgment calls (ADR prose, decision-doc history, \"renamed from X on date Y\" notes worth preserving) that benefit from being the only things left after the mechanical pass.\n\nDoing that one Sunday would have shaved a day off the following week. I have a stub for it now. Next time the registry grows a row, the script grows with it.\n\nThe shape of the lesson is small. A registry that something automated reads is worth more than a registry that only humans read. The day you rename half your projects is the day it pays for itself.\n",
  "writing/trellis-1-0-rc": "---\ntitle: \"Trellis 1.0-rc: making the process enforce itself\"\ndek: The last gap was not a missing rule. It was no guarantee the rules ran the same way in Claude Code and Codex, at every layer, on every project. Here is how I closed it.\ndate: 2026-06-03\nart: agent-process\n---\n\n# Trellis 1.0-rc: making the process enforce itself\n\n> The last gap was not a missing rule. It was no guarantee the rules ran the same way in Claude Code and Codex, at every layer, on every project. Here is how I closed it.\n\nI have written about [Trellis](/writing/trellis) twice before. The first post laid out the shape: one control plane, project inheritance through symlinks, three tiers of hooks, a fleet of weekly audits. The second was a [gap analysis](/writing/best-practices-into-trellis) against Anthropic's large-codebase guide and the ten changes I shipped to close the gaps.\n\nThis one is about a different kind of gap, and about tagging the first release candidate after I closed it.\n\nFor a while Trellis had a quiet inconsistency at its center. The rules existed, the hooks existed, the merge gates existed, and every week the audits told me when one of them drifted. But if you had asked me to prove that a given rule was actually enforced for a given project, in whichever agent happened to be driving, at the moment it mattered, I could not have given you a straight answer. I could point you at the written rule, and probably at a hook for it somewhere. What I could not promise was that it bit the same way no matter which agent was driving or which project was on the receiving end.\n\nThat is the gap the last few weeks of work closed. The result is feature-complete enough to call a release candidate, so I tagged `1.0.0-rc`.\n\n---\n\n## The enforcement matrix\n\nThe clearest way to explain the work is as a grid.\n\nDown one axis are the three layers where enforcement can happen. **Skills** are the on-demand capabilities an agent loads to do a job the right way: the spec pipeline, the process gate, the builder skill that turns a plan into commits. **Hooks** are turn-level. They fire while the agent is working, before and after edits, and at the Stop boundary where \"done\" gets adjudicated. **Gates** are the git boundary: pre-commit, commit-msg, and the pre-push that decides whether a branch is allowed to leave the machine.\n\nAcross the other axis are the two agent environments I actually run, Claude Code and Codex. They use different hook envelopes and different directory conventions, so the same policy has to exist twice, in two shapes, kept in lockstep.\n\nSix cells. The honest state a month ago was that some were solid, some were half-wired, and a couple were enforced in Claude Code but not Codex, or the reverse. A rule that blocks an edit to an unread file might exist for Claude Code and be missing for Codex. A merge gate might run the full check suite under husky and a weaker version under native git hooks. None of these were dramatic holes. They were exactly the kind of small asymmetry an audit flags as drift and that you keep meaning to fix.\n\nThe program I just finished was a systematic pass over every cell, thirteen phases, each one taking a single capability and making it identical in both clients and enforced at the right layer. Not new features. Closing the distance between a rule that exists and a rule that fires, in every cell of the grid.\n\n---\n\n## What actually shipped\n\nFour of the thirteen phases are worth describing, because they show where the cross-client work actually bites.\n\n**The re-read guard.** A PreToolUse hook, `reread-guard`, that blocks an edit to any file the agent has not read in the current session. Agents edit stale files more than you would like: the file changed under them, or they are working from a mental model two turns out of date, and the edit lands on the wrong lines. The Claude Code version was the easy half. The phase was building the Codex mirror so the same block fires whichever agent is driving, and proving both behave the same.\n\n**The builder skill.** Trellis already had the spec pipeline for planning work. What it lacked was a single load-bearing skill for the build step itself, the one that turns an approved plan into commits without stepping outside the process. Making that skill canonical, and making it resolve identically in both clients, was its own phase.\n\n**The shared merge gate.** The phase I care about most. The pre-push hook is the durable backstop I described in the first post, the thing that catches a problem even when every earlier layer was bypassed. What changed is that it stopped meaning three different things depending on how a project wired its hooks. Husky for the Node projects, native git hooks under `.githooks/` for the ones without a `package.json`, and a couple of clones, clusterbid-console among them, where the wiring had quietly drifted. The phase put all of it behind one canonical gate that runs the same process-gate check regardless of plumbing, plus a tool that re-points each project at it safely, skipping anything with a custom pre-push it does not recognize instead of stomping on it.\n\n**The doctor.** All of this needs something that can answer the question I could not answer a month ago, so the program leaned on the doctor, the two-tier health check I shipped back in 0.7 and pointed at the whole registry. Tier zero checks the control plane itself: on main, clean, synced with origin. If the source of truth is dirty, every project downstream is inheriting unversioned rules, so that check fails loud. Tier one then walks every registered project and verifies the inheritance end to end: rules resolve to canonical, the hook copies match, the settings wiring is present, the Codex parity artifacts exist, the pre-push points at the real gate.\n\n---\n\n## The part that was actually hard: rolling it out\n\nBuilding the capabilities was the bounded part. Landing them on seven projects that were all in mid-flight is where the schedule actually went.\n\nThe clean version of a rollout assumes every project is a blank slate you can overwrite. Mine are not. Of the seven projects in the registry, five were not sitting on a clean main when I went to deploy. Four were on feature branches, one had a dirty main, and three of them carried uncommitted working-tree changes. Some had settings files with their own customizations, or hook configurations hand-tuned for a polyglot monorepo. You cannot drop a canonical settings file on top of that. You would erase the project's own additions, and on neev, which carries a hand-tuned module-boundary hook that exists nowhere else in the fleet, that erasure would be unrecoverable.\n\nSo the rollout needed a merge, not a copy. The settings reconciliation reads the canonical hook wiring, applies it, then re-appends any hook entry the project added that the canonical set does not have. The project's customizations survive and the baseline lands on top. Run it twice and the second pass changes nothing. When I deployed to neev, its module-boundary hook came through untouched, sitting alongside the four new canonical entries, which is the one case where a wrong merge would have cost real work.\n\nI learned to distrust my own tests here. Every piece of the merge had unit coverage against fixtures, and all of it was green. But a settings file that is structurally valid is not the same as a settings file a real agent will load without complaint. The check that actually mattered was deploying to one real project, then starting a real agent session in it and confirming the merged configuration loaded and the turn completed. I ran one project as a canary, confirmed it end to end including a live session, and only then fanned out to the rest.\n\nFor the projects with active work in progress, the rule was strict. Stage only the infrastructure paths, diff the staged set before committing to confirm not one line of feature work crept in, commit locally, and never push. The enforcement infrastructure lands without touching the project's own branch. Whoever owns that branch finds the hooks updated and their feature work exactly where they left it.\n\n---\n\n## What I got wrong along the way\n\nThree things worth saying out loud, in the spirit of receipts over self-congratulation.\n\nA test that passes is not the same as a thing that works. I shipped a rollout tool whose tests were green while the tool itself had a logic error that only surfaced on a real merge. The tests were asserting against the shape I assumed the behavior would have, not the behavior. The fix was not the bug. It was rewriting the tests to check what the tool actually did to a real settings file.\n\nThe second one cost me a red main branch. I let a pull request merge automatically, on the assumption that branch protection would hold it until the checks passed. It merged immediately, red, because the protection had no required status checks configured. The automation did exactly what I told it to, which was nothing. Now I merge by hand after confirming green, and adding required checks is on the list.\n\nThe third was portability. A shell idiom that works on the macOS system bash, version 3.2, is a hard error on the bash 5 that the cross-platform check runs under. One harmless-looking array expression, repeated across eleven scripts, turned the whole fleet's check suite red the moment it ran somewhere other than my laptop.\n\n---\n\n## What \"release candidate\" means here, and what it does not\n\nThe matrix is complete. Every cell is enforced, in both clients, at the right layer. The doctor reports green across all seven projects and the canonical control plane. The rollout tooling has been run against every project in the registry, and the public template carries the same code the private control plane runs.\n\nThat is enough to call a candidate. It is not enough to call it 1.0.\n\nA release candidate buys soak time. Enforcement code looks correct right up until the day it blocks something it should have allowed, or waves through something it should have caught, on a project shape I did not test against. Surfacing exactly that, week after week, is the whole point of the audit fleet. So `1.0.0-rc` goes out, the audits keep running, and 1.0 is what I tag when a few weeks of real use across all seven projects produce no enforcement surprise the matrix did not predict.\n\nThe template is public and forkable at [github.com/Zireael26/trellis](https://github.com/Zireael26/trellis). If you run Claude Code or Codex at any scale, the question the last month answered for me is worth asking about your own setup. Not \"do I have a rule for this,\" but \"is that rule actually enforced, the same way, everywhere it claims to be.\" Writing that answer down honestly was most of the work. I will know in a few weeks of audits whether I got the rest right.\n",
  "writing/trellis-loop-era": "---\ntitle: \"Loops need brakes: five Trellis release candidates in five weeks\"\ndek: Everyone spent this summer telling you to stop prompting and start designing loops. I agree, and I got there backwards. The brakes came first, then the map, then the gate, then a second engine.\ndate: 2026-07-08\nart: agent-process\n---\n\n# Loops need brakes: five Trellis release candidates in five weeks\n\n> Everyone spent this summer telling you to stop prompting and start designing loops. I agree, and I got there backwards. The brakes came first, then the map, then the gate, then a second engine.\n\nIn early June, Peter Steinberger wrote that you shouldn't be prompting coding agents anymore, you should be designing loops that prompt your agents. The same week, Boris Cherny said the same thing on stage about Claude Code: he doesn't prompt anymore, he has loops running. Latent Space stitched the two together with Karpathy into a piece about loop craft, and for a month my feeds have been full of people discovering that the unit of agentic work is not the prompt, it is the loop.\n\nI believe all of it. I also think the conversation is missing its most boring and most important half.\n\nSince [tagging 1.0-rc](/writing/trellis-1-0-rc) five weeks ago, [Trellis](/writing/trellis) has gone through five more release candidates, rc.4 through rc.8, and in hindsight they form an arc I did not plan: a loop safety contract, a loop selection doctrine, a deterministic process gate, and finally a second engine to run units of the loop on. This post is that arc, receipts included, because the loop hype is real but the failure modes are too, and I have hit most of them on my own machine.\n\n---\n\n## The brakes came before the engine\n\nThe thing nobody puts in the loop threads: a loop that cannot stop is not automation, it is a liability with a token bill. My first loops-adjacent work was not about making loops smarter. It was a halting contract.\n\nEvery Trellis loop, whether it is a cron audit, a fan-out workflow, or an agent chewing on a goal overnight, declares three ceilings and halts on whichever trips first: a maximum iteration count, a no-progress count, and a dollar budget. The dollar ceiling converts to tokens at the rate of whichever engine is actually billing, which mattered later in ways I did not anticipate. On a trip, the loop hard-stops and emits a structured halt report. It never auto-continues past its own ceiling.\n\nThe no-progress ceiling earns its keep the most. Iteration caps catch runaways. Budget caps catch expensive runaways. But the common failure is a loop that is technically making calls and practically doing nothing, retrying a fix that will never land, and \"three iterations without a progress signal\" catches that shape days before the other two ceilings would.\n\nNone of this is clever. It is the seatbelt code you write before the highway, and it is the reason everything after it was safe to build.\n\n---\n\n## The map: which loop, not just how to stop one\n\nWhen the Claude team published their loops piece, I did the same exercise I did with their large-codebase guide last month: ground every claim against what Trellis already ships and be honest about the gaps.\n\nThe honest answer surprised me in both directions. On loop safety, Trellis was ahead of the blog. Ceilings, progress signals, halt reports, adversarial review of loop output, none of that appears in the published guidance in comparable depth. But on loop selection, the blog had something I simply did not: a taxonomy. Four loop types, each with a trigger, a stop signal, and the primitive you should reach for. Trellis had every primitive on that list and no map from situation to primitive. An agent deciding between a goal loop, a scheduled task, and a fan-out workflow was navigating on vibes.\n\nSo rc.6 shipped the map as doctrine: turn-based, goal-based, time-based, and proactive loops, each pointing at the Trellis primitive that implements it and each deferring to the existing halting contract rather than restating it. Selection says which loop. Safety says how it stops. Two documents, cross-linked, deliberately not merged.\n\nIt also shipped the restraint line I keep having to relearn: not every task needs a loop. Start with the simplest primitive that has a real stop condition. The orchestration-heavy default is a bias, and the counterweight has to be written down or it loses.\n\n---\n\n## The gate: loops are only as trustworthy as the process under them\n\nrc.7 was the unglamorous one. A deterministic pre-push gate: any branch whose gated diff exceeds a size floor cannot leave the machine without a spec, a plan, and a task list committed alongside it, or an explicit surgical declaration that caps the diff instead. It is a pure function of git state. No model judgment anywhere in the verdict.\n\nThe reason this belongs in the loop story is the word deterministic. I run two harnesses, Claude Code and Codex, and the gate's verdict engine is byte-identical between them. When a loop is pushing branches at two in the morning, the question \"would this have been allowed if the other agent were driving\" has to have exactly one answer. A gate that depends on which model reads the rule is not a gate, it is a suggestion with extra steps.\n\nThe control plane runs with the gate live on itself, which is how most of what follows got tested.\n\n---\n\n## The second engine\n\nrc.8 shipped this week, and it started with a skill someone else wrote. Steinberger published `codex-first`, his personal routing policy: Codex types, Claude thinks and verifies. Given that he now works for OpenAI, I wanted the neutral version, so I did the grounding before adopting anything.\n\nTwo of his load-bearing claims did not survive contact with the data. \"GPT is usually better at writing code\" is an overclaim; on the benchmarks I can actually check, implementation quality is near parity, one engine leads the harder reasoning suite and blind review comparisons by a wide margin, the other leads slightly on the broad suite. And \"Claude is metered while Codex is flat-rate\" was simply stale by July; both vendors moved automation to metered pools this spring. What survives is the interesting part: per task, one leg is several times cheaper in tokens, and if you hold two subscriptions you have two quota pools, so routing the token-heavy bulk to the cheaper pool while the quality-sensitive minority stays home is just structural sense.\n\nThen Anthropic published numbers that settled the argument from the other direction. An orchestrator on the expensive model delegating to cheap workers hit 96 percent of the expensive model's benchmark performance at 46 percent of the price. A cheap executor with the expensive model as a rarely-called advisor: 92 percent at 63. The lesson has nothing to do with vendor allegiance: call the expensive model rarely and decisively, run the bulk somewhere cheap. The cheap leg can be Codex. It can just as well be a smaller Claude worker. The topology is the point.\n\nSo the doctrine that shipped is delegation, not allegiance: a bounded work order, meaning a frozen spec, a known repro, a mechanical refactor, can route to a cheap executor from any turn. Design stays home. Review of the executor's output stays home, always, and the executor's claims are treated as advisory until I have read the diff like a contributor PR. There is a floor below which delegation overhead loses, around twenty changed lines. And for now every delegation is a proposal I approve, with a ledger and explicit criteria for when it flips to automatic, because a routing predicate I have not watched work for a week is a guess, not a policy.\n\nOne detail I fought myself on: the sandbox. The tempting move is to run the executor with everything bypassed, and half the internet will tell you that is fine. It mostly is, until the day a prompt-injected work order is holding a process with write access to your SSH keys. The version that shipped keeps approvals off, because the wrapper never prompted anyway, keeps the workspace sandbox on, enables network access in config since that is the friction that actually pushes people to disable sandboxes, and reserves the fully-unsandboxed path for disposable worktrees only. Written down in the ADR with the honest framing: sandboxless is a host-wide privilege no matter what directory you point it at.\n\nThe first real delegation under the new doctrine was the build itself. Codex wrote the sync-script guard described below, eighty lines, one round, proof attached. Ledger row one.\n\n---\n\n## What I got wrong along the way\n\nSame section as last time, same reason. Receipts over self-congratulation.\n\nThe public mirror shipped dangling references for weeks and I did not notice. The loop safety contract lives in a file the sync allowlist never included, while thirteen files that did sync referenced it. Every public fork had doctrine pointing at a document that did not exist. I found out because an agent on my own work laptop, running against the fork, flagged the missing file. Second occurrence of exactly this bug class, so the fix is a class fix: the sync now greps every published file for references and fails if any target is not itself published. The check went red on the pre-fix state, which is the only proof of a guard worth having.\n\nI taught the wrong resume command in my own plan. The delegation doctrine originally documented iterating on an executor's work by resuming a specific task id. The wrapper has no id-valued resume; the flag I documented is a boolean that resumes whatever ran last, which under two concurrent units would silently continue the wrong one and corrupt the takeover accounting. The cross-model adversarial review caught it before merge, by actually reading the wrapper's argument parser rather than trusting my plan. The doctrine now captures the real thread id at dispatch and resumes that. The reviewer being a different model family than the author is the reason this shipped correct.\n\nAnd the near-miss: I almost hand-edited the strength table the day a new model preview started trending. The published claims about which engine leads what are dated and sourced, and the only update path is re-running the research after a launch settles, precisely because launch-week numbers come with contamination flags and I have a bias toward whichever model I read about most recently. The table survived the week untouched. The re-grounding run is scheduled.\n\n---\n\n## Where this leaves the loop\n\nFive candidates in five weeks sounds like velocity. It is mostly convergence. Brakes, map, gate, second engine: each one existed because the previous one made its absence obvious, and the loop hype cycle happened to arrive while I was in the middle of it.\n\nIf you are building loops right now, the fashionable question is which loop pattern to use, and it is a good question. The unfashionable ones that decided every release above: what stops this loop, would the verdict be identical if the other agent were driving, and when the executor says it is done, who checks. My answers are in the public template at [github.com/Zireael26/trellis](https://github.com/Zireael26/trellis), dangling references now included, then fixed, with the guard to prove it.\n\nThe delegation pilot has a week of ledger rows to accumulate before it earns automatic routing. I will know by then whether the predicate is a policy or a guess, and if the ledger says guess, that goes in the next post too.\n",
  "writing/trellis": "---\ntitle: \"Trellis: An Engineering Process for AI Coding Agents\"\ndek: How I run six projects with one rulebook, nine hooks, and a fleet of weekly audits, and why I open-sourced it.\ndate: 2026-05-11\nart: agent-process\n---\n\n# Trellis: An Engineering Process for AI Coding Agents\n\n> How I run six projects with one rulebook, nine hooks, and a fleet of weekly audits, and why I open-sourced it.\n\n## The problem nobody warns you about\n\nThe first time a coding agent writes production code for you it feels like a superpower. The tenth time, you start seeing the pattern in the corners it cuts: pushed straight to `main`, marked a task done while the tests never ran, summarised what it meant to do instead of what it did, force-pushed over an hour of your work to \"clean things up.\"\n\nThese are not model bugs. They are gaps in the process around the model, the same gaps human teams closed over decades with branch protection, code review, commit conventions, CI, and a hundred smaller rituals. An agent inherits none of that by default. You have to build the scaffolding yourself.\n\nI have spent the last three months doing exactly that, in the open, across six of my projects. The result is **Trellis**: a parent/child engineering-process regime for Claude Code and Codex. One source of truth for cross-cutting rules, nine canonical hooks that make those rules mechanically enforceable, and a fleet of scheduled audits that keeps every project honest week after week.\n\nThe name is the metaphor. A trellis is the frame a climbing plant grows on. Without one the plant sprawls and breaks; with one it grows tall and clean. Same idea, applied to code an agent writes.\n\nThe public, forkable template: [github.com/Zireael26/trellis](https://github.com/Zireael26/trellis).\n\n---\n\n## The shape of the system\n\n![Trellis architecture: parent control plane, project inheritance, three-tier hook enforcement, and the scheduled audit fleet](/images/writing/trellis-architecture.svg)\n\nAt the top is a single **control plane** repo. It owns the parent rules (`core-rules/CLAUDE.md`), the hook specifications (`hooks.md`), the inheritance contract (`inheritance.md`), the registry of active projects (`registry.md`), and a `deferred.md` for rules that haven't earned their place yet.\n\nEvery project in the registry inherits from the control plane through symlinks. `.claude/rules/trellis.md` for Claude Code, `.agents/rules/trellis.md` for Codex, both pointing at the same canonical file. Projects only carry what's actually project-specific: their own `CLAUDE.md`, a `gotchas.md`, and a `context-log.md` the hooks maintain automatically across sessions.\n\nInside each project sits a three-tier hook stack. Tier one runs on every agent turn with a budget of three seconds. Tier two runs on `Stop` with ninety seconds, and that is where \"done\" gets verified. Tier three is the git boundary: pre-commit, commit-msg, and pre-push. It is the backstop if the agent slipped past everything else.\n\nEvery week, ten scheduled audits sweep the whole registry and write timestamped reports back into the control plane. Drift, bypasses, vulnerabilities, test rot, and dependency posture all surface in dated markdown I can grep, diff, and quote in a commit message.\n\nThe rest of this post is what's load-bearing about that arrangement.\n\n---\n\n## Principles I keep coming back to\n\nA few tests that kept me honest whenever I was tempted to add another rule or a clever new automation.\n\n**Parent/child layers, Rule of Three for promotion.** Cross-cutting rules belong in the parent layer. Project-specific rules belong in the project. A pattern earns parent-status only after a *third independent project* adopts it. `n=2` is the danger zone: enough repetition to feel like a pattern, not enough to rule out coincidence. Candidates wait in `core-rules/deferred.md` until they get their third witness or get demoted entirely.\n\n**Process is code.** Every rule that can be mechanically enforced becomes a hook, and hooks fail closed. Rules that depend on good intentions erode in under a month. Rules backed by a hook persist until the hook is deleted. If I can't write a hook for a rule, I question whether it should be one.\n\n**Receipts over self-reporting.** \"Done\" means the agent attaches the verification command, the exit code, and the diff lines that prove the change. \"It works\" without receipts isn't done. `stop-verify` enforces the checks; the presentation discipline is on the agent.\n\n**Small surface, deep discipline.** The parent layer is intentionally small: under 5 KB for the rules file, nine hooks, ten audits. Anything broader belongs in a project. Keeps the contract legible to humans and LLMs both, and keeps the drift surface narrow enough to actually watch.\n\n**Claude Code and Codex stay aligned.** The two agent environments use different hook envelopes, so canonical implementations live separately under `core-rules/hooks/` and `core-rules/codex/`. The policy intent is identical: same rule, same enforcement, whichever agent is driving.\n\n---\n\n## What the hooks actually do\n\nNine canonical hooks split across three tiers. The split is about budget, not capability.\n\nThe **fast-local** tier (every turn, sub-second budget) stops the catastrophic stuff before it ships. `block-destructive` denies `rm -rf` aimed at the home directory, `git push --force` on any branch, `git reset --hard` against `HEAD~N`, unscoped `DELETE FROM` SQL, and any read of a `.env*` file. `post-edit-verify` lints whatever the agent just wrote, scoped to the touched file so the budget stays under three seconds. `session-context` injects the previous session's state at startup: current branch, last five commits, dirty files, outstanding gotchas, the prior `context-log.md`. The agent doesn't have to spelunk to find where it left off. `save-context-log` and `post-compact-context` close the loop on the way out.\n\nThe **heavy-gated** tier (on `Stop`, ≤90s budget) is where \"done\" gets adjudicated. `stop-verify` enforces the receipts rule. It scans the `TodoWrite` list and blocks if anything is `in_progress` or `pending`, then runs typecheck → lint → fast tests across the whole repo. On failure the agent gets a sliced error (first 30 lines for compile errors, last 30 for test output) and is told to fix it before claiming completion. `code-review-subagent` dispatches a read-only review agent against the current diff on edit-heavy turns (≥3 files or ≥200 lines). `ui-verify` boots the dev server, screenshots the affected route, and blocks if a UI-visible change shipped without that proof.\n\nThe **git-boundary** tier is the durable backstop. Husky's pre-commit runs `lint-staged` on staged files. `commit-msg` enforces Conventional Commits with a project-configured scope allowlist. `pre-push` blocks direct pushes to `main` across every registered project and reruns the typecheck/lint/test triad. There is an emergency override (`TRELLIS_ALLOW_MAIN_PUSH=1`); every invocation has to be documented in a commit trailer or `gotchas.md`. For projects without a `package.json` (Unity, Rust, Go, or Python-only), the same enforcement lands in native git hooks under `.githooks/`.\n\nThree tiers because each one covers a different failure mode. Fast-local catches mistakes mid-turn while context is still warm. Heavy-gated catches \"claims done but isn't\" at session boundaries. Git-boundary catches anything that happened outside a Claude turn entirely: manual amends, rebases, and force-pushes from another terminal.\n\n---\n\n## The process-gate skill\n\nHooks fire automatically. Sometimes I want to ask the agent explicitly, *is this branch mergeable yet?*, and have it answer in a format I trust.\n\nThat's the **process-gate** skill. Canonical implementation under `core-rules/skills/process-gate/`, symlinked into every project's `.claude/skills/` and `.agents/skills/`. On invocation it runs six gates: PR hygiene, secrets, bypass markers, tests and coverage, docs discipline, and any stack-specific validators the project declares. Then it emits a fixed-shape verdict block:\n\n```\nPR hygiene:        ✅ pass\nSecrets:           ✅ pass\nBypass markers:    ❌ fail\nTests & coverage:  ⚠️ warn\nDocs discipline:   ✅ pass\nStack profile:     ✅ pass\n\nOverall: BLOCKED\n```\n\nOne `❌` blocks the merge. Warnings can be accepted but the acceptance has to be recorded in the PR description. The skill reads its references at invocation time, so policy changes don't require resyncing every project. Same enforcement in Claude Code, Codex, `claude -p` headless, and CI.\n\n---\n\n## Audits: the feedback loop\n\nHooks and the skill enforce policy in the moment. Audits answer the harder question: *is the policy still being enforced six weeks from now?*\n\nTen scheduled audits run on cron against the registered project list. Some hit weekly, some daily, some monthly. Different checks, but each one writes a dated markdown report back into `audits/` so I can trend over time.\n\n`cross-project-process-audit` is the headline one. Runs every Monday morning and produces a snapshot of which projects are wired correctly, which have stale hooks, which carry recent `--no-verify` bypasses, which `CLAUDE.md` files have grown past the 5 KB soft target. `parent-hook-drift` runs Sunday night, SHA256-comparing the canonical hook implementations against the deployed copies; deltas surface as drift signatures I can pull upstream or push downstream. `bypass-tripwire` is silent on clean days and noisy when someone used `--no-verify` or shipped a direct push. `test-health` actually runs each project's fast suite. `dep-vulnerabilities` and `dep-currency` cover security and lockfile age. `gotchas-rollup` is the Rule-of-Three engine: it aggregates project-local `gotchas.md` entries and promotes anything that has hit three independent projects into the parent rules.\n\nThe audits aren't only observability. They're the reason the system stays disciplined. Every Monday I get a report telling me, in plain text, where my engineering process has drifted from its own claims. Then I fix it. The week after, the audit confirms the fix or surfaces the next drift. The loop is the thing.\n\n---\n\n## What I'd tell anyone trying to build this\n\nFour things I didn't appreciate when I started.\n\nProcess work pays back. Every hour spent making the system catch its own drift bought back ten over the following month. The pre-push guard alone has caught at least three force-push-to-main attempts that would have cost half a day each.\n\nThe Rule of Three matters more than any individual rule. I deleted several \"obviously good\" parent rules in the first month because the second project disagreed about the right shape. Three witnesses isn't bureaucracy; it is the cheapest defence against locking in the wrong abstraction.\n\nReceipts beat eloquence. I trust the diff and the exit code. I no longer trust a trailing summary that says \"I've fixed the bug and all tests pass.\" Half the work of building Trellis was unlearning the instinct to read summaries and believe them.\n\nOpen-source it early. Putting Trellis in public forced me to write `engineering-process.md`, the narrative manual that explains *why* each rule exists. Future-me reading it six months from now is the most important audience the repo has.\n\n---\n\n## Try it\n\nMIT-licensed and forkable: **[github.com/Zireael26/trellis](https://github.com/Zireael26/trellis)**\n\n`AGENT_SETUP.md` walks any Claude Code or Codex session through customising it for your machine in about ten minutes. `AGENT_ONBOARD_PROJECT.md` is what you paste inside the bootstrapped repo to onboard each project after that. The audit fleet ships pre-registered as scheduled-task prompts; wire them into whatever scheduler you already use.\n\nIf you fork it, tell me what you changed. The Rule of Three needs three independent witnesses, and right now I am only one of them.\n",
};
