# Handoff

_Written 2026-08-23, ~02:20 IST, at the end of an unattended L4 run._

## Where things are

**The site is deployed and live at https://beta.akaushik.org**, running on
Cloudflare Workers via `@opennextjs/cloudflare`. Branch `feat/pixel-transplant`,
tip is the last commit in `git log`. Nothing is pushed and no PR is open.

`akaushik.org` is untouched and still serves from Vercel. Pointing the apex at
Workers is the cutover (T34) and is a decision to take awake — ADR-0018 puts a
14-day paused-not-deleted window on the Vercel project after it.

## The thing to know before touching anything

Three Cloudflare Workers constraints bit during the deploy, all recorded in
ADR-0019 and in `docs/CHANGELOG.md`:

1. **No Node middleware.** Next 16 removed the edge runtime from `proxy.ts`;
   OpenNext supports nothing else. The agent-readiness contract lives in
   `lib/agent-proxy.ts` as pure functions, with `proxy.ts` (local) and
   `worker/index.ts` (Cloudflare) as thin adapters. Change policy in the pure
   module, never in one adapter.
2. **No filesystem.** `lib/content.ts` reads from
   `lib/content-bundle.generated.ts`, produced by
   `scripts/build-content-bundle.ts`. The generated file is committed.
3. **No runtime code generation.** No `new Function`, no `eval`, no
   WebAssembly instantiated from bytes. MDX compiles during the build via
   `scripts/build-mdx-modules.ts` into `lib/mdx/generated/` (git-ignored,
   regenerated by `prebuild`, `pretypecheck` and `pretest`). If you add an MDX
   plugin, add it in that script — `lib/mdx-options.ts` no longer renders
   anything.

## Commands

- `pnpm cf:build` — parks `proxy.ts`, builds the Worker, restores it. If it
  ever leaves `proxy.ts.cf-parked` behind, move it back by hand.
- `pnpm cf:deploy:preview` — build and deploy to `beta.akaushik.org`.
- **Every Wrangler call needs `env -u CLOUDFLARE_API_TOKEN`.** The zone-scoped
  token in the shell profile shadows the OAuth session Wrangler actually has.
  No token needs minting; this is the whole obstacle.

## Verified on the deployed preview

21 routes answer; both Markdown negotiation patterns work; the nonce CSP
reaches every script tag; all six discovery `Link` rels are present; `POST
/api/mcp` returns a real `tools/list`; all three OG card shapes were downloaded
and inspected; preview is refused by both `X-Robots-Tag` and `robots.txt`.
Vitals: LCP 448–1180 ms, CLS ≤ 0.002.

## Open, in rough priority order

- **T34 cutover** — operator decision.
- **T31 Lighthouse has not run.** It is CI-only and not installed locally. Only
  direct vitals were measured, which is weaker than the budgets.
- **T32 `isitagentready.com` has not run** — it cannot reach a `noindex`
  preview. Run it after cutover.
- **T27 Experience section is thin.** It needs sourced CV detail. Nothing was
  invented and nothing should be.
- **T03 open questions** — OQ1 (display face) and OQ3 (hyperframes on case
  studies) are still unanswered.
- **T39 hero art** — the training animation works; whether it is *good* is a
  taste call nobody has made.
- **T28 copy pass** against `docs/voice.md` across the new sections.

## The foreman

An OMP foreman runs in `../akaushik.org-worktrees/pages` on branch
`feat/pixel-pages`, pane `w4:p9`. Phases 1 and 2 are merged. Phase 3 (e2e
specs, a11y sweep, ADR-0019, dead comment references) was dispatched and is
mid-flight; check `git -C ../akaushik.org-worktrees/pages log` before assuming
anything landed. It has now gone idle with uncommitted work three times —
always check its working tree, never trust "done".

## Decisions taken without you

`decisions-log.md` at the repo root. Ten entries, D1–D10.
